name: summ
type: atomic
kind: linguistic
input:
  input_as: text
  input_type: json
  input_schema:
    type: object
    properties:
      text:
        type: string
output:
  output_as: summ
  output_type: json
  output_schema:
    type: object
    properties:
      summ:
        type: string
prompt: 'Summarize: {text}'
