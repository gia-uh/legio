name: assess
type: atomic
kind: tool
input:
  input_as: extract
  input_type: json
  input_schema:
    type: object
    properties:
      title:
        type: string
      summary:
        type: string
output:
  output_as: result
  output_type: json
  output_schema:
    type: object
    properties:
      result:
        type: string
tool: assess
parameters:
  title: '{extract.title}'
  summary: '{extract.summary}'
