# 2026-09-14 — Session 86a — LEG-095 Phase 1

## Scope
LEG-095 (instance state report, LEG-095 contract): Phase 1 — report-convergent
lifecycle; agent out-side intake; Runtime correlation + intake drain; registry
posteriori. Maintenance of LEG-087/LEG-088/LEG-085 runtime tests (message
model) forced by RED-first. LEG-087/LEG-095 contract doc amendments landed here.

## Work done (per issue)

### LEG-095 — Phase 1 (report-convergent lifecycle)
- **flow.control**: `STATE_REPORT_SCOPE`, `STATE_REPORT_MESSAGE_TYPE`,
  `ReportedState` (PARKED/READY/TERMINATING), `AgentStateReport` envelope
  (frozen, `extra=forbid`, `schema_version` default SCHEMA_VERSION,
  `message_type` Literal["state_report"], `instance_id`, `action`, `seq` ge=1,
  `state`) — all exported from `legio.flow`.
- **agents.base**: `_deposit_state_report` — unsigned report per honored
  control at neutral priority on the report intake scope; failure is a visible
  `logger.exception` that does **not** change the honor decision. Deposits after
  ENABLE→READY, DISABLE→PARKED, TERMINATE→TERMINATING.
- **runtime**: `STATE_REPORT_TASK` intake fact (`_state_report_fact`): pops one
  report, `AgentStateReport.model_validate` (intake scope, rule 8), correlates
  against the `_pending_controls` ledger keyed `(instance_id, action, seq)`.
  Orphan/mismatch/unknown-instance → WARNING, **consumed, never applied**.
  READY→ENABLED / PARKED→DISABLED via the Registry posteriori; TERMINATING
  informational. Re-kicks the drain while reports remain.
- `enable_instance`/`disable_instance`/`enable_class` converge on the state
  report (`_await_instance_state`), bounded Registry read, **no optimistic
  write** — backlog kept, never minted.
- `destroy_instance` (`DESTROY_INSTANCE` fact, LEG-087): structural terminal;
  on bring-up SUCCESS → `_purge_pending` + kick `STATE_REPORT_TASK`; drain of
  the terminating report is informational.
- `_purge_pending(instance_id)`: removes pending mint(s) for the instance on
  destroy (the drain converges on the structural terminal — a report that never
  arrives is, structurally, the record's terminal; §LEG-087 §5.7/LEG-095 §4.6).

### LEG-087 / LEG-088 / LEG-085 (message-model maintenance caused by RED)
- **ONE-SHOT → MOUNTED** vehicle across the runtime test files: the one-shot
  bring-up model is deprecated for lifecycle verbs; tests now mount a real
  `ToolAgent` (mounted via a bring-up runner). `tool_agent`/`_mounted_tool_agent`
  helpers + `_pool_executor` added in test_leg085_runtime; the `_pool` flag in
  tests; `test_leg088_node_ops.py` `_mounted_tool_agent` for gate-probe.
- Control-queue/`seq`/`signature` assertions updated to the **report** model:
  verify the agent's loop, correlates controls.

## Logging added
- runtime.error: link to LEG-095/LEG-087

## Decisions taken
- **Report-convergent confirms (LEG-095 §4.5/§5.8)**: enable/disable converge on
  the *state report*, not on an ack. Registry posteriori (report applied on
  intake correlate). No optimistic write on the lifecycle path (rule: mint →
  correlate → posteriori). **No ack by design** (LEG-087 §4.3/§4.8). Destroy
  waits on the structural terminal (bring-up record `success`).
- Orphan/mismatch state reports are **consumed with a visible WARNING** (rule 9
  — never silently dropped), never applied.

## Tests run
- `tests/test_leg085_runtime.py` + `tests/test_leg088_node_ops.py`: 42 passed.
- Full suite: `375 passed` (ruff + pyright green).

## Known issues / open points
- LEG-095 DRAFT: awaiting maintainer approval (issue #50). Phase 2 (result
  queue), Phase 3 (db proxy / federated deposit) deferred.
- The unused-import lint (`import logging as _logging`) in the probe was
  removed; PROBE artifacts cleaned.

## Next steps
1. Wait for maintainer approval of LEG-095 (DRAFT) before Phase 2.
2. Update the LEG-095 contract (§5 future phases: per-submit result queue) with
   the approved report intake as the structural stem.

---

# 2026-09-14 — Session 86b — LEG-095 approved + Phase 2 implemented (per-agent result queue + outbox intake)

## Scope
LEG-095 (R-8): the maintainer ordered steps 1+2 ("haz los pasos 1 y 2") —
i.e. Phase 1 approval plus Phase 2. This turn records the in-session approval
of Phase 1 (same precedent as 85z), writes the Phase 2 spec contract-first
(docs before code, rule 12), implements it red-first, migrates every affected
test, and amends the stale `result:<task_id>` wording across contracts and
ARCHITECTURE.

## Work done (per issue)

### LEG-095 Phase 1 — approved
- Contract status flipped to APPROVED (maintainer, this turn); PLAN.md LEG-095
  bullet extended with the Phase 2 accept lines. GitHub issue numbers pending
  (Phase 1 #50 + a Phase 2 number — maintainer opens, same as 85z).

### LEG-095 Phase 2 — spec (contract-first)
- New "Phase 2" section in `docs/CONTRACTS/LEG-095-instance-state-report.md`
  (approved in-session): per-agent `result:<agent>` queue, `RESULT_DRAIN`
  intake fact, kick sources (submit + kick-on-miss reads), record-only
  `status`/`read_outbox`/`ack_outbox`, `destroy_class` clearing, logging,
  accept, red-first test list.

### LEG-095 Phase 2 — implementation (green)
- **naming**: `result_queue_key(agent_name)` → `result:<agent>` (one queue per
  served root agent, shared across tasks); new `OUTBOX_SCOPE = "outbox"` +
  `outbox_key(task_id)` → `outbox:<task_id>` (the `result_key` pointer).
- **runtime**: new Manager fact `RESULT_DRAIN_TASK = "result_drain"` (pop one,
  validate, record, re-kick while items remain; invalid item → WARNING,
  consumed, never applied; no gate check — a computed result never strands);
  `self._outbox = db.dict("outbox")` (footprint now
  `gates`+`node_ops`+`state_report`+`outbox`); `submit`/`submit_work_item` seed
  `end_of_level_queue = result:<starting-agent>` and kick the drain;
  `status`/`read_outbox` kick iff the record is absent (kick-on-miss, agent
  from the seed's launcher); `status` completes only via the record
  (`result_key = outbox:<task_id>`); `ack_outbox` consumes the record only
  (never kicks); `destroy_class` also clears `result:<name>` (records
  survive); `__all__` gains `RESULT_DRAIN_TASK`.
- **agents/flow/api**: untouched (the token carries the queue name opaquely;
  the HTTP shells keep endpoints/codes) except a `_guard_outbox` docstring.
- **tests**: new `tests/test_leg095_result_drain.py` (13: naming, per-agent
  seeding, collect→COMPLETED, two-task isolation, re-kick drain, invalid-item
  WARNING, ack/empty/ack-without-poll semantics, unknown task, destroy
  clearing, fact registration, rule-13 footprint pin). Migrated to the record
  model: 014/025/026/027/032/043/044/081/085/093 + flow_integration
  (kick → pump → read; `result_key` → `outbox_key`; outbox-dict assertions).
- **docs amended**: ARCH §2/§7, AGENT_LIFECYCLE §5.8/§12.5.3, PLAN LEG-026
  accept, LEG-014/016/024/026/032/050/085/088/093/094-pending contracts,
  VALIDATIONS single-node-model, LEG-088 pin comment scoped to its scenario.

## Logging added
- `runtime submit` / `work_item` INFO now carry `result_queue=result:<agent>`.
- `runtime result_drain task=… agent=… recorded=true` (INFO, hit);
  `runtime result_drain invalid agent=… reason=…` (WARNING, consumed);
  `runtime destroy_class result_queue cleared class=… items=…` (INFO).
  Read/ack INFO lines kept (`read_outbox ready=true`, `ack_outbox`).

## Tests run
- RED first: new module failed collection (`cannot import name 'outbox_key'`).
- GREEN: 13/13 new; affected files green; full suite **388 passed ×2 runs**
  (375 prior + 13 new); `ruff check .` clean (1 import-order autofix);
  `pyright` 0/0/0.
- One `test_leg087_bring_up::test_real_bring_up_fails_visibly_when_agent_not_mounted`
  failure in the first full run of this turn; passes standalone and in both
  subsequent full runs — same timing-flakiness family as the 85u `test_leg088`
  note, unrelated to this change (that path submits nothing).

## Decisions taken
- **Kick-on-miss, not kick-always**: `status`/`read_outbox` schedule a drain
  only when the record is absent — bounds Manager-task growth during poll
  loops; `ack` never kicks (pure ack-without-poll returns `False`, losing
  nothing — the next poll collects).
- **Drain ignores gates** (a computed result must never strand); **destroy
  keeps outbox records** (armageddon ends the class, not readable history);
  **`result_key` = `outbox:<task_id>`** (where the result lives post-collection).
- **Beaver lists only non-empty queues**: discovered via the footprint pin —
  fully-drained queues leave no residue (asserted, not assumed).
- Phase 2 stays under the LEG-095 umbrella (85y "contract amendments"); a
  separate GitHub issue number is the maintainer's call.

## Known issues / open points
- GitHub issues pending maintainer: LEG-095 Phase 1 (#50) + Phase 2 number;
  LEG-090/091/092/093 still open for maintainer close.
- LEG-094 DRAFT still pending the directed-model refresh; Phase 3 (db proxy +
  federated deposit) is next per the 85y roadmap.

## Next steps
1. Maintainer review + close of LEG-095 (Phase 1 + Phase 2).
2. **Phase 3** — contract draft (node-injected db proxy + federation deposit
   endpoint), red-first, then green.
3. LEG-094 multi-node example on the Phase 2+3 surface, then LEG-081 (CLI).

---

# 2026-09-14 — Session 86c — The leg087 intermittent failure: transient RUNNING race, fixed in the Manager

## Scope
The maintainer rejected the 86b journal's "known flakiness" dismissal of the
single `test_real_bring_up_fails_visibly_when_agent_not_mounted` failure —
rightly: a sometimes-failing test hides a real race. This turn diagnoses it,
reproduces it deterministically, and fixes the root cause (a production bug,
not a test bug).

## Diagnosis (root cause, verified)
- The Manager's `_dispatch` marked **every** task RUNNING *before* driving its
  callable. For parked async generators that makes RUNNING observable during a
  window (RUNNING-write → first drive → FAILED-write) in which the vehicle was
  never alive.
- The Runtime's bring-up confirm (`_await_observable_state`, ok =
  RUNNING|SUCCESS) samples that window: if it reads between the two writes,
  `create_class` on an **unmounted** class falsely succeeds (phantom instance
  recorded, gate opened) and the test fails with DID-NOT-RAISE instead of the
  expected "no standing agent mounted".
- Narrow window (a few beaver awaits), hit only when the pump is desuspended
  mid-dispatch under full-suite load — hence 1 failure in ~4 full runs, green
  standalone. A first naive RED attempt (concurrent sampler, no widening)
  passed on buggy code, confirming beaver awaits rarely suspend in a quiet
  loop; the window was then widened deterministically (test-only delay on the
  RUNNING persist), which fails on buggy code every time and passes on fixed
  code structurally.

## Work done (per issue)
- **LEG-083 (Manager)**: `_dispatch` no longer pre-marks async generators —
  the generator is created parked in-process and `_drive_parked` writes
  RUNNING (+ started_at) only after the first yield lands. RUNNING means
  *parked*, never a transient; a stillborn generator goes PENDING → FAILED
  with no observable RUNNING. Plain callables unchanged. Blast radius checked:
  the only RUNNING-accepting waiter is the bring-up confirm; the destroy path
  waits terminal SUCCESS; pause-before-start still resumes identically
  (record PENDING until parked).
- **Tests**: `tests/test_leg083_manager.py` gains `test_stillborn_generator_never_observes_running` (deterministic RED on old code, GREEN on fixed) and `test_parked_generator_observes_running_while_parked` (steady state preserved).
- **Contracts amended**: LEG-083 dispatch algorithm (RUNNING-means-parked +
  stillborn rule); AGENT_LIFECYCLE §5.2 bring-up paragraph (same rule, 86c).

## Logging added
- None needed: the existing `manager running task=…` INFO now fires at park
  time (same event, truer moment); `manager failed` exception logs already
  cover the stillborn path.

## Tests run
- RED: stillborn test fails on old code (`RUNNING in seen`); GREEN after fix.
- Full suite **390 passed ×2** (388 + 2 new); the 4 most timing-sensitive
  files (083/085/087/088) green across 6 consecutive runs; `ruff` clean
  (2 UP043 autofixes); `pyright` 0/0/0.

## Decisions taken
- **Fix production, not the test**: accepting transient RUNNING as "vehicle
  up" could record phantom instances and open gates for agents that never
  live — the test was correctly pinning the contract, the Manager was
  violating it.
- RUNNING→SUCCESS early-accept stays benign (a parked-then-completing vehicle
  was alive); only RUNNING→FAILED without a park was the lie, now structural.

## Known issues / open points
- (Carried) GitHub issues pending maintainer: LEG-095 Phase 1 (#50) + Phase 2
  number; LEG-090/091/092/093 still open; LEG-094 DRAFT pending refresh.

## Next steps
1. Maintainer review + close of LEG-095 (Phase 1 + Phase 2) and of this
   Manager fix (LEG-083 amendment).
2. **Phase 3** — db proxy contract draft, red-first, then green.

---

# 2026-09-14 — Session 86d — LEG-095 Phase 3 implemented (node-injected db proxy + federated deposit)

## Scope
LEG-095 Phase 3 — the db proxy + federation deposit transport of the 85y
roadmap item 3 (directed model, session 85w). This turn: the contract was
written contract-first (docs before code, rule 12) and approved in-session by
the maintainer (the spec header records "approved in-session 86d"); the
red-first tests were authored; the implementation went green; the affected
docs and security pin landed. No new dependency (httpx already approved for
nodes), no new beaver scope (rule 13), agents untouched (zero code changes).

## Work done (per issue)

### LEG-095 Phase 3 — spec (contract-first, approved in-session 86d)
- New "Phase 3" section in `docs/CONTRACTS/LEG-095-instance-state-report.md`:
  Goal (no direct remote write ever; agents keep one `db` handle — the proxy is
  a transparent `AsyncBeaverDB`), §A `NodeDB` routing rules, §B
  `POST /deposits` + `Runtime.deposit_remote`, §C boot wiring (always wrap the
  agents' db; rosters injected or fetched fail-fast), §D non-goals (debt:
  gather-return to a composite served on both nodes misroutes locally; return
  stamps are LEG-094), §E untouched, Interface, acceptance, tests red-first,
  logging. `docs/PLAN.md` LEG-095 bullet + `LEG-017-security.md` L1 map gain
  the Phase 3 accept / `POST /deposits` row.

### LEG-095 Phase 3 — RED (`tests/test_leg095_db_proxy.py`, 22 tests)
- `build_routes` (local-first, first-peer-wins, empty);
- proxy transparency with no routes (raw-beaver behavior; `isinstance`
  holds; local put/get/count/peek + `dict("gates")`);
- remote agent deposit / gather routing via `POST /deposits` lands on the
  owner, nothing local;
- `result:<task>` origin routing (peer author → owner; self → local;
  malformed/legacy → local; unknown well-formed origin → visible
  `RecoverableError`);
- foreign reads (`get`/`peek`/`count`) raise; owner refusal (disabled class →
  `class_disabled`, nothing deposited) raises; unreachable owner raises;
- `/deposits` pins: 401 no/wrong token, 404 absent without federation, 503
  `no_capacity`, 422 bad shape + `extra="forbid"` junk, 404 `unknown_agent`,
  409 `class_disabled`, 200 lands agent/gather/result queues;
- `fetch_peer_catalogs` parsing + fail-fast visibility (respx);
- cross-node drills through a real `ToolAgent` advance (A→B and B→A via ASGI
  owner apps, no far-side vehicle);
- boot composition (injected roster → `routes == {"xray": NODE_B}`; no peers →
  pure-local proxy). One broken assertion in the fetch test fixed during RED
  (`set(rosters[NODE_B].agents)` → `set(rosters) == {NODE_B}`).
- `test_leg017_security.py`: `POST /deposits` joins the L1 map pin.

### LEG-095 Phase 3 — GREEN
- **`legio.federation`**: `NodeDB(AsyncBeaverDB)` — subclasses without opening
  a second connection (shares the raw db; `isinstance` holds; `__getattr__`
  delegates everything else; only `queue` is name-routed; `dict` delegates);
  routing per the spec (result origin parse via `_TASK_ORIGIN_RE`;
  agent/gather via the static table; unprefixed scopes local always);
  `RemoteQueue` — read-less shim (`put` → `POST {owner}/deposits` with L1
  bearer, non-dict refused, transport/refusal → visible `RecoverableError`
  with peer + code; `get`/`peek`/`count` raise); `build_routes(local_served,
  peer_rosters)` (duck-typed rosters — LEG-090 `CatalogResponse` or plain
  iterables); `fetch_peer_catalogs(peers, token)` fail-fast naming the peer.
- **`legio.runtime`**: `Runtime.deposit_remote(queue, item, priority)` — the
  owner's local put; gate-checks agent/gather queues against the Runtime's own
  `gates` scope (result queues put purely — a computed result must never
  strand); refuses names outside `legio:queue:`.
- **`legio.api`**: `POST /deposits` (mounted iff federation token; L1 guard →
  401; validation order 401 → 503 `no_capacity` → 422 `invalid_request` →
  404 `unknown_agent` → 409 `class_disabled` → 200 `{queue, deposited}`);
  `DepositRequest`/`DepositResponse`; `_deposit_agent` helper (result queues
  are pure transport, never served-checked).
- **`legio.materializer`**: `boot_node`/`_boot_on_database` always wrap the
  agents' db in a `NodeDB` (injected `peer_catalogs` or HTTP `fetch_peer_catalogs`
  when peers configured + token present; peers-without-token warns and stays
  local); Runtime/Manager/Registry keep the raw db.
- **ARCHITECTURE.md §9/§10**: message-level transport paragraph + `/deposits`
  endpoint in the L1 list.

## Logging added (rule 11, with the implementation)
- `federation deposit remote queue=… peer=…` (INFO, cross-node leg traced);
  `federation deposit remote failed/refused peer=… queue=… code=…` (WARNING);
  `federation catalog fetch failed/refused peer=… url=…` (WARNING),
  `federation rosters fetched peers=…` (INFO).
- `api deposits queue=… deposited=true` (INFO); `unauthorized`/`invalid_request`/
  `unknown_agent`/`denied` (WARNING); `no_capacity` (ERROR — a federated node
  must never serve silently empty).
- `runtime deposit_remote queue=…` (INFO); `deposit_remote refused/denied
  queue=…` (WARNING).
- `materializer` warns when peers are configured without
  `LEGIO_FEDERATION_TOKEN` (the proxy stays local, rule 9).

## Tests run
- RED first: module failed collection (`cannot import name 'NodeDB'`).
- GREEN: 22/22 new; related suites (017/090/091/092/093/081/085/095 state +
  drain) green; full suite **412 passed** (390 prior + 22 new); `ruff check .`
  clean; `pyright` 0/0/0.

## Decisions taken
- **Subclass without a connection**: `NodeDB` extends `AsyncBeaverDB` but never
  calls `AsyncBeaverDB.__init__` — it shares the raw db's state and delegates
  by `__getattr__`, so the proxy opens no second connection and never owns the
  lifecycle (only `queue` is routed; `dict` and everything else stay local).
- **Routing is pure name-only** (no I/O at route time): `result:<task>` parses
  the author origin; malformed/legacy → local back-compat; unknown well-formed
  origin → visible error (a genuine routing failure must never strand).
- **Endpoint served-checks exclude result queues** (pure transport), mirroring
  the drain's never-strand invariant; the gate check is the Runtime's own
  `gates` scope, never the API layer's.
- **Peer-without-token boots local** (documented WARNING): peers are "dormant
  until federation" (FederationConfig docstring); only a configured token
  triggers the HTTP fetch.
- Legacy `result:<task_id>` names keep routing local (back-compat) — the debt
  note of §D records that a delegated *final* step returning to an
  agent-named `result:<agent>` stays local (return stamps are LEG-094).

## Known issues / open points
- (Carried) GitHub issues pending maintainer: LEG-095 Phases 1 (#50)/2/3
  numbers; LEG-090/091/092/093 close; LEG-094 DRAFT pending the directed-model
  refresh with the Phase 3 surface as its transport.

## Next steps
1. Maintainer review + close of LEG-095 (Phases 1–3) and the 86c Manager fix.
2. **LEG-094** — the multi-node directed example on the Phase 2+3 surface
   (fan-out delegation + return stamps), then LEG-081 (CLI).
