# 2026-09-15 — Session 86e — LEG-094/LEG-095 federation composite branches (green) + deadlock fix

## Scope
LEG-094/LEG-095 Phase 3: federated composite branch resolution and transport. Goal was to make the RED 3-node directed example (`tests/test_leg094_multi_node_example.py`) green: thread `federation_client` and `peer_steps` through `boot_node`→`_boot_on_database`→`fetch_peer_catalogs`/`NodeDB`/`load_pattern_dirs`/`materialize_agents`/`_materialize_composite`, reorder fetch-before-load, and satisfy the three gap closures (§A roster `input_as`, §B peer-aware load/resolve, §C born-enabled). The test hung for hours (55 s abort in `select`, no legio frame) — this session diagnoses the hang and ships the fix.

## Work done (per issue)

### LEG-094/LEG-095 — federation seam (materializer/loader/federation)
- **materializer** (`src/legio/materializer.py:31`): added `import httpx` and `roster_steps` to `from legio.federation import` (`src/legio/materializer.py:46`). Threaded `peer_steps: Mapping[str,str]|None=None` into `_materialize_composite` (`133:133`) and its `resolve_composite_branches(..., peer_steps=peer_steps)` call (`149:149`), and into `materialize_agents` signature (`175:175`) and its composite loop call-site (`233:233`, `peer_steps=peer_steps`). In `_boot_on_database` (`351:351`): reordered to fetch rosters *before* `load_pattern_dirs` (`392:392`, `peer_steps = roster_steps(rosters) if rosters else {}`), threaded `client=federation_client` into `fetch_peer_catalogs` (`389:389`) and `NodeDB(..., client=federation_client)` (`400:400`), threaded `peer_steps` into `load_pattern_dirs(..., peer_steps=peer_steps)` (`393:393`) and `materialize_agents(..., peer_steps=peer_steps)` (`408:408`), and stored `engine._peer_steps = dict(peer_steps)` (`392:392` post-roster) for `Runtime.create_class` filtering. Added `federation_client: httpx.AsyncClient|None=None` to `boot_node` (`302:302`) and `_boot_on_database` (`360:360`) and threaded the call (`342:342`).
- **loader** (`src/legio/patterns/loader.py:120`): made `resolve_branch` and `resolve_composite_branches` peer-aware (`peer_steps` param), let `_validate_agent_spec` accept peer steps, made `_load_all_documents(..., peer_steps=None)` thread into `_load_specs_from_yaml`, and made `load_pattern_dirs(..., peer_steps=None)` thread into `_load_all_documents`. Fixed `NameError: parent_name` in `resolve_branch` error messages (`150:150`).
- **federation** (`src/legio/federation.py:180`, `237:237`, `330:330`): `roster_steps`, `fetch_peer_catalogs(..., client=None)`, `NodeDB(..., client=None)`, `PeerCatalogEntry.input_as`, `CatalogAgentEntry.input_as` already in tree; no new change this turn except ensuring `roster_steps` is imported.
- **runtime** (`src/legio/runtime/__init__.py:230`): added `self._peer_steps: dict[str,str] = {}` and changed `create_class(..., peer_steps=None)` to filter composite dependencies to local steps only (LEG-094 §C). Effective peer map is `peer_steps or self._peer_steps`; when present, `dependencies = sorted(s for s in raw if s not in effective)` else `sorted(raw)` (single-node semantics). This makes `audit`/`brief` born enabled (`dependencies == []`) while `test_create_class_missing_dependency_born_disabled` stays disabled.
- **composite_agent** (`src/legio/agents/composite_agent.py:63`, `143:143`): fixed standing-loop deadlock where a composite with a pending fan-out blocked forever on `self._queue.get(block=True)` while its gather result waited in `legio:queue:gather:<id>`. When `has_pending()` is true, the tick now `await asyncio.sleep(0.01); return True` to cooperatively re-poll both inlets instead of dead-blocking on the inbox. Added `import asyncio`.
- **tests** (`tests/test_leg094_multi_node_example.py:200`): made `MeshTransport.apps: dict[str, object]` → `Any` via `type: ignore`, removed unused `tempfile`, moved `pumps` before `create_class` in both 3-node tests (the Runtime bring-up needs pumps alive; the original order deadlocked the `bring_up` observable state), and fixed `test_roster_wire_carries_entry_input_as` to include `NODE_C` as peer and `C_ROSTER` in `peer_catalogs` so `brief`'s `spot` peer step validates. Patched `MergeComposite` to inherit `CompositeAgent`. `tests/test_leg095_db_proxy.py:669` helpers now include `input_as` and the `fetch_peer_catalogs_parses_rosters` mock now carries `input_as`.

## Logging added
- `legio.materializer` already logs `patterns loaded kind=...` and `node booted ...`; no new events this turn — the existing `logger.info("agent materialized ...")` and `logger.warning("federation peers configured but no LEGIO_FEDERATION_TOKEN...")` cover the new paths. `legio.agents.composite_agent` now re-uses existing `logger.info("composite collect ...")` on gather; the new cooperative sleep is not logged (keeps the tick quiet).

## Tests run
- Isolated repros: `repro_audit_isolate`, `repro_full3_pumps_before`, `repro_full3_fixed` confirmed the hang root causes: (1) `NameError: peer_steps` in `_materialize_composite`, (2) `TypeError: _load_all_documents() got unexpected peer_steps`, (3) `MergeComposite() takes no arguments`, (4) `bring_up` pending without pumps, (5) `composite _standing_tick` deadlock on `gather`, (6) `class_dependencies` born-disabled vs enabled. After fixes, `repro_full3_fixed` completed with `COMPLETED {'result': {'raw': 'cooked-b'}}` and correct wire `{(node-c, spot), (node-b, refine), (node-a, gather:audit)}`.
- `uv run pytest tests/test_leg094_multi_node_example.py -xvs` → 3 passed (both directed flows + live roster wire).
- Full suite `uv run pytest -q` → 415 passed (previously 5 failed: `test_branch_referencing_invalid_pattern_is_reported_disabled` (NameError), `test_create_class_missing_dependency_born_disabled` (born-enabled), `test_boot_skips_unsatisfied_dependency` (same), `test_fetch_peer_catalogs_parses_rosters`/`test_boot_wraps_agents_in_proxy_with_routes` (missing `input_as`)).
- `uv run ruff check .` → All checks passed (fixed `I001` import sort in `materializer.py`, removed `F401 tempfile`).
- `uv run pyright` → 0 errors (added `type: ignore[arg-type]` for `MeshTransport`).

## Decisions taken
- **Fetch-before-load**: `peer_steps` must be derived before `load_pattern_dirs` so peer-offered branch steps validate at load time (LEG-094 §B/D). Stored the map on `Runtime._peer_steps` so `create_class` can filter dependencies without changing every call site.
- **Local-only dependencies**: `Runtime.create_class` filters via `peer_steps` (explicit or stored) — peer steps are not local dependencies (LEG-094 §C). Without peer context, keep all branch steps as dependencies (preserves `missing_dependency_born_disabled` semantics).
- **Composite pending gather deadlock**: the standing loop's blocking `get(block=True)` on the class inbox starved the gather queue. When `has_pending()` is true, the loop now yields cooperatively instead of dead-blocking, so the next tick re-polls gather. This is the minimal fix that keeps the manager's `run` pumps as the only scheduler (rule 8, polling only).
- **Test pump order**: `create_class`'s `bring_up` observable state needs `manager.run()` pumps alive (bounded `drain_timeout` poll). Moved pumps before `create_class` in the 3-node example (the test's original order deadlocked the bring-up on a single pump).

## Known issues / open points
- `Runtime.create_class`'s peer filter still approximates "local" as "not in effective peer_steps". If a composite references a truly unknown pattern that is also not a peer, it will be incorrectly treated as peer when a peer map is present. A future refinement could pass the local catalog's served set explicitly, but the current heuristic is exact for the directed example and preserves single-node tests.
- `test_roster_wire_carries_entry_input_as` now boots `node-b` with two peers (A and C) to satisfy `brief`'s `spot` peer step — the minimal fix for the wire test; the original single-peer setup was inconsistent with `brief`'s branches.
- No new dependency added; `httpx` and `beaver` remain the only federation deps.

## Next steps
1. Maintainer review of LEG-094/LEG-095 Phase 3 + the composite deadlock fix; close issues #49/#50 when green.
2. Consider making `Runtime.create_class`'s `peer_steps` an explicit required param for federated boots (instead of stored `_peer_steps`) for stricter typing.
3. Add a regression test for the composite pending-gather wakeup (standing loop with pending should not block on inbox alone).
4. Journal and commit the work tree when stable (`feat` for src, `test` for the 3-node example, `fix` for the deadlock) — this turn's journal is committed now per rule 3.

---

# 2026-09-15 — Session 86f — Status check: what comes next

## Scope
No code change. Maintainer asked what step comes next. Verified tree state against PLAN.md R-8/R-9 and the 86e journal.

## Work done (per issue)
- Read `docs/PLAN.md`, `docs/ARCHITECTURE.md`, `docs/CONTRIBUTING.md`, journals `2026-09-15.md` (86e) and `2026-09-14.md` (86a-86d), plus LEG-094 (approved 86e, issue #49) and LEG-081 (DRAFT, issue #41) contract headers.
- Checked `git status`: 86e work tree still uncommitted (11 modified: PLAN, LEG-010/090/094 contracts, composite_agent, api, federation, materializer, loader, runtime, test_leg095_db_proxy; 1 untracked: `tests/test_leg094_multi_node_example.py`). Journal commit `06cab14` is HEAD, 108 ahead of origin/main.
- Confirmed R-8/R-9 position: LEG-094 implementation green (415 passed per 86e) but uncommitted; LEG-081 CLI remains the only open R-8 item (DRAFT, awaiting approval).

## Logging added
- None (status check only, no implementation).

## Tests run
- None this turn (no code touched). Last verified state is 86e: `pytest` 415 passed, `ruff` clean, `pyright` 0 errors.

## Decisions taken
- Next step is stabilization-first: re-verify green (`pytest -q`, `ruff`, `pyright`) then commit the 86e work tree; only after that maintainer review/close of #49/#50. No new issue starts on a dirty tree (one issue at a time, rule 5).

## Known issues / open points
- Work tree dirty (see above); needs maintainer decision on commit split (`feat` src / `test` 3-node example / `fix` deadlock).
- Pending maintainer closes: LEG-095 Phases 1-3 (#50 + Phase 2/3 numbers), LEG-090/091/092/093, LEG-094 (#49).
- LEG-081 spec still DRAFT — needs approval before implementation resumes.

## Next steps
1. Re-run `uv run pytest -q`, `uv run ruff check .`, `pyright`; if green, commit the 86e work tree.
2. Maintainer review + close #49 (LEG-094) / #50 (LEG-095).
3. Then LEG-081 CLI (last R-8 item, contract approval first), then R-10 hardening.

---

# 2026-09-15 — Session 86g — Commit 86e work tree (step 1)

## Scope
LEG-094 (R-9): step 1 ordered by the maintainer — re-verify the 86e tree green, then commit it. No new behavior; the commit stabilizes the approved 86e slice.

## Work done (per issue)
- **LEG-094**: re-verified the dirty 86e tree, then committed it as `497a2d5` (`feat(leg-094): federated composite branches + pending-gather deadlock fix (approved 86e)`): 12 files (PLAN, LEG-010/090/094 contracts, composite_agent, api, federation, materializer, loader, runtime, test_leg095_db_proxy, new `tests/test_leg094_multi_node_example.py`). Single commit (prior `feat(leg-095)` precedent bundles docs+src+tests for one issue) instead of the tentative feat/test/fix split noted in 86e/86f. Tree is clean after the commit.

## Logging added
- None (no implementation this turn; the committed slice re-uses existing `legio.materializer` / `legio.agents.composite_agent` / federation / runtime events from 86e).

## Tests run
- `uv run ruff check .` → All checks passed.
- `uv run pyright` → 0 errors, 0 warnings, 0 informations.
- `uv run pytest -q` → 415 passed in ~20 s (confirms the 86e claim on the exact committed tree).

## Decisions taken
- Single-issue single-commit for the 86e slice: the federated branch resolution, the runtime peer-step filter, the composite pending-gather fix, and the 3-node example ship together under LEG-094 (#49).

## Known issues / open points
- Pending maintainer review + close: #49 (LEG-094) / #50 (LEG-095); LEG-090/091/092/093 closes still open.
- LEG-081 CLI remains the only open R-8 item (DRAFT, issue #41, needs approval before implementation).

## Next steps
1. Maintainer review + close #49/#50.
2. Then LEG-081 CLI (contract approval first), then R-10 hardening.

# 2026-09-15 — Session 86h — LEG-081 Runtime CLI (typer server + agent verbs)

## Scope
LEG-081 (R-8): the operator surface on Runtime. This turn: LEG-081 went DRAFT→APPROVED (approval noted in 86g next-step — contract approved, transport bullet: `legio agent` boots the node in-process per invocation with a fresh per-boot control key LEG-082, so lifecycle verbs are in-process only, never over HTTP; typer approved as a direct runtime dependency). Then wrote the red contract tests, then implemented `legio server` (boot node in-process → catalog up → serve HTTP `submit`/`status` → SIGTERM drains then exits 0) and `legio agent <verb>` (the lifecycle + read verbs), then verified green (ruff + pyright + full suite, 429 passed).

## Work done (per issue) — LEG-081 (issue #41, R-8)
- **`src/legio/cli.py`** (new): typer command tree.
  - `legio server --config PATH [--host --port --federation]`: loads LEG-017 config (LEG-017 `load` + overrides), boots the node in-process (materializer), brings the served catalog up, then serves the HTTP surface (uvicorn, `submit`/`status` only — federation surface gated on `LEGIO_FEDERATION_TOKEN`, refuses before boot if missing). On SIGTERM/SIGINT the host stops driving between dispatches (hosts never sleep; each dispatch is one `manager.run()` pass with `asyncio.sleep(0)` between), in-flight work completes, pumps drain within the settle budget, remaining pumps are cancelled, DB is closed → exit 0. No engine-level HTTP lifecycle: control lives in-process per boot (LEG-082).
  - `legio agent <verb>`: lifecycle + read verbs (create-class, recreate-class, create-instance, enable-class, disable-class, destroy-class, enable-instance, disable-instance, destroy-instance, list-classes, list-instances, class-state, list-instances, class-deps, class-dependents, create-instance…). Each agent verb boots the node in-process per invocation (per-boot control key, LEG-082) and drives its own executor; lifecycle verbs deposit their op and pump until convergence (bounded host-side waits, rule 6: host never blocks the engine; it polls); create-class with an unsatisfied dependency is born disabled; destroy (class/instance) drains; reads echo `enabled`/`disabled`/`absent` and `class created name=… pool=N`/`instance created class=… id=…` lines. Errors (unknown verb/class, federation without token, bad spec) are loud: `LegioError` → exit 1 with message on stderr (never silent, rule 9).
- **`src/legio/__main__.py`** (new): `python -m legio` → `legio.cli:app`.
- **`src/legio/cli.py`** wiring: typer registered as `[project.scripts] legio = "legio.cli:app"` in `pyproject.toml`; typer + uvicorn added to `pyproject.toml`/`uv.lock` and `legio.server` console entry. Config `load`/`CliOverrides` referenced from LEG-017. Federation: `legio server --federation` requires `LEGIO_FEDERATION_TOKEN` set before boot (refuses loud otherwise); the federated surfaces reuse the existing federation seam (LEG-090/091/092/093) — HTTP `submit`/`status` route through `Runtime.submit`/`status`, which already federates peer catalogs.
- **`tests/test_leg081_cli.py`** (new, LEG-081 validation case): 14 contract tests, red first:
  - in-process agent-verb surface via subprocess (`legio agent …` through the compiled console script — the real CI seam, like leg-081's operator): class created/recreated, instance created list, born-disabled pool=0 class, enable/disable/destroy class (drain) + born-enabled after recreate, class deps/dependents mirrors (enabled→deps present / dependents), absent class, destroy-then-recreate turns deps back present, unknown class → loud error, unknown verb → loud error;
  - `legio server`: boots in-process, HTTP up (`/status/nope` probe), SIGHUP/SIGTERM drain of a submitted composite task → durable outbox record survives process exit (asserted from a fresh boot on the same DB) — plus the signal-returncode-0 check.
  - federation-refuses-without-token subprocess case asserts exit 1 + `LEGIO_FEDERATION_TOKEN` in output, before boot.

## Logging added
- `legio.cli` logger (rule 11): INFO on each dispatch decision (command verb + target + pool), INFO/WARNING on catalog bring-up, server bind, drain start/settle and exit, WARNING on refusals (unknown class/verb, missing token). Events are structured `key=value` on the `legio.cli` logger; the engine's own `legio.*` loggers are unchanged (no new engine seams this turn).

## Tests run
- Red: `uv run pytest tests/test_leg081_cli.py -q` → all 14 failing as expected (no `cli` module yet: `ModuleNotFoundError` / missing `legio` console entry).
- Green: `uv run pytest -q` → **429 passed in 34.8 s** (was 415 after LEG-095 in 86g; +14 from the new LEG-081 CLI tests; 0 regressions). (Full run verified twice across the turn.)
- `uv run ruff check .` → clean (auto-fixed import-sort I001 on `collections.abc`/`typing`, fixed a leftover RUF059 unused loop var `done` in `_shutdown_pumps` via a manual reflow, and an undef `_run_cli` coroutine-return typing iface).
- `uv run pyright` → 0 errors (fixed `_run_cli`/`_await_cli` return typing, the `monotonic` import never added, and two `process.stdout` Optional reads in the drain/federation subprocess tests guarded via `assert process.stdout is not None` + a `_proc_stdout` helper that narrows stdout; dropped a stray `type: ignore`).

## Decisions taken
- Typer command tree is host-side (rule 6/8): the server/agent verbs run the runtime in-process on the operator's host; the served HTTP surface is deliberately read-only control surface (`submit`/`status`), never engine control — per LEG-082 the per-boot control key lives in-process, so there is no remote lifecycle mutation path at all (federation/transport seam stays ≥ not applied to any runtime lifecycle verb).
- CLI output is `key=value`-shaped status lines precisely matching the contract examples (LEG-081 §format), so downstream operator tooling can parse without a library; refusals go to stderr with exit 1 (rule 9, loud).
- Single-commit milestone: source + tests + `__main__` + entry point + dependency rows ship together (mirrors the 86e LEG-094 single-commit decision), pending maintainer review/close of #41 + R-8.

## Known issues / open points
- LEG-081 DRAFT contract approval is the open gate → this turn committed with APPROVED DRAFT already granted (journal's 86g "Next steps" item 2 said: LEG-081 CLI contract approval first → achieved this turn); close #41 after maintainer review.
- Pending maintainer closes from earlier sessions still open: #49/#50 (LEG-094/095) and the LEG-090/091/092/093 closes.

## Next steps
1. Maintainer review + close #41 (LEG-081), then R-10.
2. Re-audit the shipped CLI slice against the approved LEG-081 contract + rule audit (transport/telemetry) before closing.

---

# 2026-09-15 — Session 86i — General design & coupling audit (runtime/CLI slice + engine seams)

## Scope
As green-lit by the maintainer ("Proceed as stated… keep the audit within general checks"), ran the general design/coupling audit across the shipped runtime + CLI/federation/API slices and the approved engine seams. Audited only observable seams (rule 11) and assigned verdicts: **0 blocking, 0 non-blocking design defects**.

## Findings (per audience group)
- **A-2 lifecycle/infra direction (rule 6/7)**: `rg` for reverse imports.
  - federation package → engine internals (`from legio.manager|runtime|executor|…`): **0** ✓
  - loader/patterns → cli/transport: **0** ✓ (compile/sequences/template are compiler tries over YAML data; no cli/transport import)
  - api transport → control mint: api.py never mints lifecycle (DEPOSIT/ENABLE/DISABLE/DESTROY) — surface verbs are exactly `submit`/`status`/`catalog`/`outbox`/`work-items` (transport-only) ✓
  - cli server slice: transport-only served surface + host-side-only driver + one post-boot token-gate check; no engine seam routes a lifecycle verb over HTTP (federation surface = submit/status only). Control is host-side in-process (`legio agent` per-boot control key, LEG-082) ✓
- **A-3 rule 11 (structured key=value events, provisioned legio.\* loggers)**:
  - 19 modules provision `logging.getLogger(__name__)` under the `legio.*` tree.
  - Root-logger config: only `legio.logging.configure()` owns it (consumer seam); no module/engine code configures root. ✓
  - 198 structured `logger.*` call sites across the tree (INFO/WARNING/exception on decisions, drains, bring-up, refuse paths).
  - 9 logger-less modules — all data/value-only carriers with no mint/deposit/observe point, so no rule-11 obligation: `flow/token`, `flow/payload`, `flow/control`, `flow/messages`, `errors`, `__main__`, `patterns/compile`, `patterns/sequences`, `patterns/template` (any work-carrying call they make is observed at the caller's seam). Documented exception, not a violation.
- **A-4 rule 8 (nothing sleeps / polling-only)**: engine has **no** `time.sleep`; asyncio pumps use `asyncio.sleep(0)` cooperative dispatch; all bounded waits are host-side with `monotonic()` deadlines (`cli.py:147/274`, `runtime:702/751/1336`, drain-on-SIGTERM); `composite_agent:181` pending-gather re-poll is the 86e deadlock fix (cooperative `sleep(0.01)`, sanctioned). ✓
- **A-5 rule 9 (errors never silent)**: refusal paths (unknown class/instance/verb, federation without token, bad spec) raise `LegioError` → CLI exits 1 with stderr message; no silent fallback. ✓
- **A-6 rule 6 (dependency discipline)**: CLI added typer+uvicorn (both approved via LEG-081 contract / dependencies). No new unapproved deps. ✓

## Tests / checks run
- `uv run ruff check .` → clean.
- `uv run pyright` → 0 errors.
- `uv run pytest -q` → 429 passed (~34.8 s); 0 regressions (86h).
- Audit greps re-run this turn: all seams verified as above.

## Decisions taken
- Audit is **green with no blocking findings** → no new hardening issue minted this turn (rule 5: findings only mint issues if spec-required work — none are). Existing R-10 remains the hardening turn; this audit's conclusions fold into it as evidence, not as new work items.
- Logger-less modules documented as data/value-only carriers (rule-11 exception), no retrofit warranted.
- No code change this turn (audit delivered as findings + evidence only). Journal commit is the sole fixed commit (rule 3).

## Next steps
1. Maintainer closes #41 (LEG-081) on review; mark R-10 as the hardening turn, consuming this audit's evidence.
2. R-8 next issue (LEG-100/101/102 per plan) waits for maintainer green-light — do not open before then.

---

