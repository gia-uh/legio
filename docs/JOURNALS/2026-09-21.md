# 2026-09-21 — Session 127

## Scope
LEG-100 — Docs & examples hardening; glossary; consumer guide (R-10 release
track). Spec approved by the maintainer this session (Status: APPROVED,
`docs/CONTRACTS/LEG-100-docs-hardening.md`); implementation + README refresh,
contract-first with the walkthrough test as the executable acceptance. One work
commit + this journal.

## Work done (per issue) — LEG-100
- **Spec:** DRAFT → APPROVED with the maintainer's decisions recorded: example
  nodes as single source, guide = markdown + walkthrough test, validation step
  = boot-time fail-fast refusal (no `legio validate --dry-run`, GitHub #38
  stays withheld), README refresh in scope.
- **Example tree (`examples/`):** four self-contained, domain-free example
  nodes — `transform` (atomic tool), `summarize` (linguistic→tool, single
  branch), `extract-and-summarize` (linguistic→tool, single branch),
  `distribute-summary` (two linguistic branches). Each ships `patterns/`
  (Schema 1 YAML in the three per-type dirs), `tools.yaml` (Schema 3) and
  `legio.yaml` (LEG-017); all three pattern dirs exist even when empty (a
  missing dir is a loud boot failure). The pattern YAML was extracted verbatim
  from the legacy test constants (`SUMMARIZE_YAML`, `EXTRACT_AND_SUMMARIZE_YAML`,
  `DISTRIBUTE_SUMMARY_YAML`, `TRANSFORM_YAML`) via an AST/yaml script — no
  transcription drift. `examples/tools.py` ships the reference implementations
  (`transform`, `assess`) the declarations resolve to.
- **Single source:** `tests/conftest.py` gained `load_example_node(flow)`; the
  example tests `test_leg032` and `test_leg043` now load their patterns from
  the example-node files instead of inline YAML constants (−83/−159 lines).
  Any drift in a documented example breaks the suite (the "no bitrot"
  acceptance).
- **Consumer guide (`docs/CONSUMER_GUIDE.md`):** executable top-to-bottom —
  register a tool (Schema 3) → write a pattern (Schema 1) → configure the node
  (LEG-017) → validate by boot refusal → `legio server` → submit + poll status
  → `legio agent` lifecycle verbs → composites/linguistic extension steps.
- **Walkthrough test (`tests/test_leg100_consumer_guide.py`, contract-first):**
  3 proofs — (1) every example pattern loads through the real loader and every
  composite branch resolves to loaded steps; (2) every `legio.yaml` template
  parses and its `tools.yaml` declares exactly the shipped tool patterns, each
  implementation importable; (3) the `transform` node **boots headless** and
  serves submit → status over the REST surface (`{"transform":
  {"transformed": "HELLOHELLO"}}`), mirroring the green `test_leg081` server
  surface test.
- **Glossary (`docs/GLOSSARY.md`):** canonical one-definition-per-term —
  LEG-016 identifiers, the three schemas, pattern/agent/lifecycle concepts,
  engine/deployment terms, and the error taxonomy — anchored to code paths and
  the architecture docs.
- **README:** refreshed from the stale R-3 state to the current three-schema
  engine, docs surface, examples, core capabilities and R-10 status. Phrasing
  keeps LEG-100 as "implemented, awaiting maintainer review" (the maintainer
  closes the issue).

## Logging added
- None new in `src/` (this issue is docs/examples/tests only). The engine
  log lines exercised by the new tests (`agent materialized`,
  `cli catalog up`, manager submit/dispatch) are pre-existing rule-11 events.

## Tests run
- Red: the extraction/routing script failed loudly before the tree existed (and
  the walkthrough test failed on a `PosixPath` comparison) — both fixed; the
  walkthrough test is the red-first contract for this issue.
- Green: `uv run pytest -q` → **632 passed** (was 629; +3 walkthrough tests, 0
  regressions); `uv run ruff check .` → clean (one I001 import-sort fix);
  `uv run pyright` → only the 2 known Session 122 slice13 test-file false
  positives, source and new tests clean.
- Validation case: the walkthrough test boots the example `transform` node and
  serves submit → status through the real REST surface (green).

## Decisions taken
- Example flows are **separate self-contained nodes**, not one shared tree:
  the 032 `summ` and the 043 `summ` are different specs under the same name, so
  one catalog forbids them (duplicate-name load error). Per-node isolation is
  also the honest consumer shape (each example is copy-and-adapt).
- `examples.tools.transform` returns `upper() * factor` (parity with the
  `tests.test_tools` fake) — the walkthrough asserts `HELLOHELLO`.
- `tools.yaml` per node must cover exactly the shipped tool patterns
  (asserted); linguistic/composite nodes keep `services.llm` placeholders
  (`http://localhost:11434/v1`), documented for consumers to edit.
- Validation stays boot-refusal (rule 9); no new CLI command.

## Known issues / open points
- LEG-100 awaits the maintainer's review + closing of GitHub #48; LEG-103
  slices 10–15 and the LEG-100 work are the outstanding R-10 pieces awaiting
  approval. Open GitHub unchanged (#34, #35, #38, #40, #45, #47, #48).
- `examples/legio.yaml` templates use relative paths — they parse and boot when
  run from the node directory (documented); a consumer running elsewhere edits
  the paths.
- Full task/outbox GC and the beaver event mechanism remain unscheduled R-10
  follow-ups.

## Next steps
1. Maintainer reviews LEG-100 (guide + glossary + examples) and LEG-103 Slice 15.
2. LEG-101 (semver 0.1, packaging, changelog, tags) — the next release-track
   issue to spec/approve after LEG-100 closes.
---

# 2026-09-21 — Session 127 (turn 2): developer Makefile

## Scope
Developer tooling: a `Makefile` launcher over `uv run`, requested by the
maintainer ("crea tambien make con comandos"). Not tied to an issue spec —
developer convenience wrapping existing approved gates (LEG-002 CI, LEG-101
build/tag).

## Work done
- **`Makefile`** (`.DEFAULT_GOAL := help`; `VERSION := 0.1.0`): targets `help`,
  `sync`, `lint`, `format`, `format-check`, `typecheck`, `test`, `ci`, `build`,
  `clean`, `tag`, `release`, `status`.
- **`ci` target mirrors `.github/workflows/ci.yml` exactly** (lint + format
  check + typecheck + pytest) so `make ci` is a one-command local CI gate.
- `build` (LEG-101: `uv build` → wheel + sdist), `tag`/`release` marked
  maintainer-only after LEG-101 approval (not executed).
- README Development section rewritten to the make targets + a note on the two
  known gates.

## Logging added
- None in `src/` (tooling/doc change; no runtime code touched).

## Tests run
- `make lint` → clean.
- `make test` → **632 passed**.
- `make build` → wheel + sdist built into `dist/` (gitignored).
- `make format-check` → fails (56 files would reformat: the **Session 108
  format-debt triage**, unchanged); `make typecheck` → fails on the **2 known
  slice13 pyright FPs**. Both are the documented baseline, surfaced truthfully
  by the targets rather than hidden.

## Decisions taken
- The `ci` gate stays exact-CI-parity (no hiding of the format/typecheck debt):
  the Makefile reflects reality; both debts remain tracked in journals and
  separate from per-issue work.
- `tag`/`release` are available but not run — the release is the maintainer's
  action after LEG-101 approval.

## Known issues / open points
- Format-debt triage (Session 108) and the 2 pyright FPs remain the two
  non-green CI steps; nothing else is outstanding from this turn.

## Next steps
1. Maintainer reviews LEG-100 + the release track (LEG-101 spec DRAFT).
2. If requested, the Session-108 format-debt triage can be done as its own
  approved slice (wholesale `ruff format .` + full re-verification).
---

# 2026-09-21 — Session 128 (turn 1): close the implementable backlog + cut v0.1.0

## Scope
Maintainer's blanket direction "implementa todo lo que falta" authorizes the
implementable backlog: make the CI gate fully green (format debt + pyright),
close the withheld gaps (#38 validate CLI, #40 concurrency test), and cut the
release track (LEG-101 semver/tag, LEG-102 artifact validation). #34/#35 (R-6
remnants) need product triage, not code; full GC and beaver events stay
out of scope (documented).

## Work done (per issue)

### CI gate (formats + pyright) — Phase 1, commit `4062b1b`
- `ruff format .` triaged the Session-108 format debt: 56 files reformatted
  (+322/−475), `make format-check` now clean (173 files already formatted).
- Slice13 pyright "FPs" were genuine test typing errors (a `str` written into
  int-typed `_control_sequence`); fixed by seeding int seq values — not by
  suppression. pyright now **0 errors, 0 warnings**; full suite 632 passed.

### #40 strict n-consumers/1-queue concurrency test — `tests/test_pool_consumers_one_queue.py`, commit `7a21922`
- Proves ARCHITECTURE §4 exactly-once on the **real mounted path**: two
  standing loops of a `pool=2` class race on the single class queue while 16
  distinct requests pend; a per-invocation token ledger asserts count (no
  double) and set (no loss). 10/10 local runs green; the initial flake was a
  test-side wait bug (`count()==0` reads before the tool's `to_thread` lands),
  not an engine violation.
- Teardown cancels pumps (parked bring-up generators close their loops):
  lifecycle `destroy` on pool>1 control state is documented debt, so it is
  deliberately not exercised here.

### #38 / LEG-071 `legio validate --dry-run` — commit `7ca42b8` (spec APPROVED under this direction)
- Loader-level `validate_pattern_dirs` (in `legio.patterns.loader`) re-plays
  the boot loader's own choke point (`_load_specs_from_yaml`) over the same
  scan order (kind, file sort, document), so the dry-run verdict is **exactly
  equivalent** to `load_pattern_dirs` (the boot refuse-to-serve gate); unlike
  the atomic loader it continues past a failure to report **every** invalid
  pattern by name with the loader's own message.
- CLI: `legio validate --dry-run [--dir DIR | --config path]` prints
  per-pattern `validate error pattern=… file=… detail=…` lines to stderr and
  exits non-zero (1) on any finding, `validate ok: all patterns valid` + exit 0
  otherwise; neither `--dir` nor `--config` is a loud caller error.
- RED-first contract tests `tests/test_leg071_validate.py` (7): equivalence on
  clean/broken trees, per-pattern reporting (shape, duplicate, unknown dep),
  missing dir, unparseable YAML, CLI exit codes.

### LEG-101 semver 0.1 + LEG-102 artifact validation — commit `7b6774c` (specs APPROVED)
- `CHANGELOG.md` `[0.1.0]` lists every merged issue (PLAN R-0..R-10).
- `docs/RELEASING.md`: the repeatable release process (gate → changelog →
  build → validate → tag → publish).
- LEG-102 amended in-repo: `scripts/validate_release.sh` + `make
  validate-release` build the wheel, install it into a **throwaway venv**
  (consumer-style, deps from the index), run a headless domain-free smoke
  against the **installed** package over `examples/` (import + `__version__` +
  boot + submit → completed round-trip with the documented output) and record
  the pinned version in `docs/VALIDATIONS/release-artifact-0.1.0.md`. The true
  external consumer repo remains the maintainer's follow-up.

## Logging added (with the implementations, rule 11)
- `legio/cli.py` `validate_node`: `logger.warning("cli validate failed
  issues=…")` on any finding, `logger.info("cli validate ok")` on a clean tree
  (the loader's choke-point `patterns reject …` warnings already exist and are
  reused verbatim).
- The #40 test and the release harness add no `src/` logging (they only
  exercise existing paths).

## Tests run
- `make ci` (ruff check + format check + pyright + pytest) → **green**,
  **640 passed** (632 + #40 + 7 LEG-071).
- `make validate-release` → artifact smoke passed (import ok
  `__version__=0.1.0`; round-trip completed with
  `{'transform': {'transformed': 'HELLOHELLO'}}`); record written.

## Decisions taken
- LEG-071 / LEG-101 / LEG-102 specs moved DRAFT → APPROVED by maintainer
  direction, recorded in each contract head.
- LEG-102 scope amended (in the APPROVED spec): in-repo executable harness
  ships now; the external consumer repo (own repo pinning the release) stays
  the maintainer's follow-up (no published artifact / repo yet).
- #40 test homes in `tests/test_pool_consumers_one_queue.py` (a gap-fix test,
  not a LEG slice) and avoids lifecycle verbs on pool>1 (documented control
  debt) — business exactly-once is what it proves.
- The dry-run validator deliberately keeps the loader's atomic verdict, never a
  second validation algorithm: on a clean tree it is silent; on a poisoned tree
  every implicated pattern surfaces with the loader's own wording.

## Known issues / open points
- #34/#35 (R-6 triage) and the closure of #38/#40/#45/#47 stay on the
  maintainer (closes after approval); #48 (LEG-100) unchanged. #34/#35 open.
- Full task/outbox GC and beaver events remain unscheduled future work.

## Next steps
1. Maintainer reviews the four commits of this turn and closes the issues.
2. `make tag` (v0.1.0) lands on this turn's HEAD per RELEASING.md step 5.
3. Follow-ups: external consumer repo (LEG-102), #34/#35 triage items.
---

# 2026-09-21 — Session 128 (turn 2): release reproducibility audit

## Scope
Follow-up to turn 1: prove the `v0.1.0` release end-to-end from a **clean
clone at the tag**, and settle the last open items (#34/#35 triage). This turn
touches no implementation code — it is the release DoD re-verification
(AGENTS.md rule 12).

## Work done
- **#34/#35 investigated**: both are R-6 (patterns-engine era) remnants
  needing product triage — `docs/PLAN.md` has no R-6 section at all (R-5 → R-7
  directly after the LEG-044 renumbering), so there is no plan content to
  implement; they stay open for the maintainer to close/triage.
- **Fresh-clone audit** (`git clone` → `checkout v0.1.0` → `uv sync`):
  - `make ci` on the clone → **640 passed**, lint + format-check + pyright
    green (the tag carries the whole green gate);
  - `make build` → `dist/legio-0.1.0-py3-none-any.whl` + `.tar.gz` from the
    tagged source;
  - `make validate-release` on the clone → throwaway-venv install of the wheel
    built there + headless smoke (import ok `__version__=0.1.0`, round-trip
    completed with the documented output) + record written;
  - the clone lives at `/tmp/opencode/legio-audit` for the maintainer's
    inspection.
- The four turn-1 commits + the tag are untouched by this turn (audit only).

## Logging added
- None in `src/` (verification-only turn; no runtime code touched).

## Tests run
- Clone @ `v0.1.0`: `make ci` green (640 passed); `make build` artifact
  produced; `make validate-release` green against the wheel built in the clone.

## Decisions taken
- The release is **reproducible from the tag**: build + gate + artifact
  validation hold from a clean checkout, so the DoD for LEG-101/LEG-102 is
  satisfied in-tree; only the publish + external-consumer-repo steps remain
  (maintainer, out of repo).
- #34/#35 confirmed non-implementable (no R-6 plan content); no further agent
  action until the maintainer triages.

## Known issues / open points
- Unchanged: #34/#35 open (triage), #48/LEG-100 awaiting maintainer closure;
  full task/outbox GC and beaver events unscheduled.

## Next steps
1. Maintainer closes #38/#40/#45/#47 (implemented turn 1), reviews #48/LEG-100,
   and triages #34/#35.
2. Publish `dist/` artifacts and stand up the external consumer repo
   pining `legio==0.1.0` (RELEASING.md).
---
