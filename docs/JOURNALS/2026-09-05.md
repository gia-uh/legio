# 2026-09-05 — Session 37 — Correct R-6 direction: resilience is idempotency + visibility, not lease/heartbeat/reaper

## Scope

Correct the direction I (the implementing agent) proposed in Session 36's "Next
steps", which re-introduced `LEG-060 Leases + heartbeat + reaper`. The maintainer
made clear this was me re-inventing a model that directly contradicts the
approved design, and that he had been explicit about it all along. Per AGENTS.md
rule 12 and the Session 16b principle ("decisions live in text, not addenda"),
this turn records the correct course and verifies the tree carries no residual
trace of the invented mechanism.

## Work done

- **Verified the working tree is clean of the invented model.** Grep over `src/`
  and `tests/` finds no `lease`/`heartbeat`/`reaper`/`retry`/`re-queue`/
  `requeue`/`_attempts`/`_is_due`/`_take_due`/`RetryGuard`/`executor_died`
  mechanism — only the intentional absence statements ("no lease, no retry and
  no re-queue") in `base.py`/`errors.py` docstrings and their tests. The
  Session 16 removal is intact.
- **Confirmed docs carry the approved design.** `ARCHITECTURE.md` §8 ("nothing
  sleeps and nothing is leased; resilience is idempotency ... and visibility,
  not at-least-once execution guards", "a raised step is surfaced, not re-run")
  and `AGENT_LIFECYCLE.md` ("Dispatch has no lease or reaper ... No reaper, no
  `executor_died` recovery, no retry/DLQ — those invented mechanisms are
  removed") state the ruling. `PLAN.md` has **no R-6 rasante** (removed in
  Session 16) — the only place the invented "R-6 Leases + heartbeat + reaper"
  reappeared was my Session 36 journal proposal, not the plan.
- **Corrected the direction in text.** This journal entry supersedes the
  Session 36 "Next steps" that proposed `LEG-060 Leases + heartbeat + reaper`.
  R-6 is not re-opened as a dispatch-lease/reaper/retry mechanism. The approved
  resilience model is: **idempotency** (an item deposited again produces the
  same outcome), **visibility** (rule 9 — a raised step is surfaced as an
  `error` result, never silently re-run), **`next_run_at` as a real-time
  scheduling field** (it gates *when* a task runs, is not a dispatch lease),
  and the **tolerant fan-in** already shipped (a child failure conserves its
  `branch_id` and yields a final result with error/partial content per the
  pattern's policy). Any future resilience work is built on these — never on
  lease/heartbeat/reaper/re-queue in the dispatch.

## Logging added

- None. Contract/docs-only correction; no code or runtime event changed
  (rule 11 — no new observable point was introduced).

## Tests run

- No code/test change this turn. Baseline from Session 35: 135 passed,
  ruff clean, pyright 0 errors.

## Decisions taken

- `LEG-060` is **not** "Leases + heartbeat + reaper". If a next resilience step
  is needed, it is specified against the approved model (idempotency +
  visibility + `next_run_at` scheduling + tolerant fan-in) and discussed with
  the maintainer contract-first (rule 12) before any code — never re-inventing
  the hostile-worker lease/reaper/retry mechanism that Session 16 removed.
- The PLAN.md absence of an R-6 rasante is correct and stays; no R-6 section
  is re-added to the plan.

## Known issues / open points

- Pending maintainer word on whether any concrete resilience work is desired
  this phase, and if so which (against the approved model).
- The implemented R-4/R-5 span is closed (#20-#30); 19 issues remain open,
  R-7..R-10.

## Next steps

- Commit this journal (regla 3) within this turn.
- Await maintainer direction: continue at R-7 (LEG-070 cascade invalidation) or
  author an approved-model resilience step, under rule 12.
