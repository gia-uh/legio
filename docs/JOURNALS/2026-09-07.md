# JOURNALS — entry template

Filename: `docs/JOURNALS/<YYYY-MM-DD>.md`. Append chronologically when working
several turns in the same day. Always fill in English. This is how development
resumes between sessions: the latest entry must say exactly where things stand.

---
# 2026-09-07 — Session 75 — Runtime LEG-083/084/085/086: Manager, Registry, Runtime public face, boot catalog (contract review + implementation start)

## Scope

R-8: LEG-083 Manager (generic task environment), LEG-084 Registry (mirror), LEG-085 Runtime public face, LEG-086 boot catalog. All DRAFT contracts; no code committed yet. Goal: review contract alignment with ARCHITECTURE/AGENT_LIFECYCLE and find coupling.

## Work done (per issue)

- **LEG-083**: contract reviewed; surface and footprint match AGENT_LIFECYCLE §6.1. Beaver scopes `tasks`/`pending_tasks`/`control` only; callable registry in-process; `run()` non-blocking poll; no layer-private business scope.
- **LEG-084**: contract reviewed; posterior mirror, reads only; writes only what Runtime confirms (queue fact → record_class → bring-ups → record_instance). No DAG/routing.
- **LEG-085**: contract reviewed; design findings (see below).
- **LEG-086**: contract reviewed; topological order + pools config wiring via `create_from_catalog`.

## Contract findings (LEG-085)

1. **Coupling — Runtime writes business records into Manager-owned `tasks` scope** (LEG-085:158: "stage ... under the Runtime's own `business_tasks` scope (never the Manager-owned `tasks`)" but code was writing into `tasks`). Real issue: the Runtime was meant to own `business_tasks` but the implementation used Manager `tasks`. Fix needed: either create `business_tasks` scope or re-home submit onto Manager.
2. **Coupling — Runtime reads Manager's `control` dict directly** (LEG-085:135 says "Runtime reaches Manager's `control` scope only through `control_mode`" but code was reaching directly). Must use public API `control_mode`.
3. **Drain parameter naming/unit drift** — contract says `drain_interval_ms` (default 100 ms); code uses `drain_interval` float seconds (0.05 s). Alignment needed before approval.
4. **Footprint-pinning test** — test that no layer writes another layer's scope (Manager tasks / Runtime gates / Registry catalog). Not yet written.

## Tests run

- None (analysis only). Tree unchanged.

## Known issues / open points

- Finding 1: Runtime's business scope (`business_tasks` vs Manager `tasks`).
- Finding 2: Runtime's direct `control` read vs public API.
- Finding 3: `drain_interval_ms` vs `drain_interval` alignment.
- Finding 4: footprint-pinning test.

## Next steps

1. Commit this journal (rule 3) — done.
2. On maintainer call: apply fixes for findings 1-3; add footprint test; then implement.

---

# 2026-09-07 — Session 76 — Runtime LEG-083: Manager implementation (tests first)

## Scope

Implement LEG-083 (`legio.manager.Manager`) per approved contract. Tests first (red), then implementation (green).

## Work done (per issue)

- **LEG-083**: `tests/test_leg083_manager.py` written (contract tests); `src/legio/manager/__init__.py` implemented: `Manager(db, node_id)`, `register`, `submit_task`, `status`, `control_mode`, `pause`, `resume`, `cancel`, `run` (one polling pass). Beaver scopes: `tasks`, `pending_tasks`, `control`. Callable registry in-process. Task executor algorithm as specified. Logging at all observable points (INFO submit/success, DEBUG dispatch, WARNING fail/cancel/deny, exception on crash).

## Tests run

- `uv run pytest tests/test_leg083_manager.py` — 27 passed.
- `uv run pytest -q` — 162 passed (no regressions).
- `uv run ruff check .` — All checks passed.
- `uv run pyright` — 0 errors.

## Decisions taken

- Manager's `submit_task` signature: `submit_task(name, *args, **kwargs)` returns `task_id`. `task_id` is minted as `<node_id>:<uuid>`. No explicit `task_id` param yet (LEG-085 will add it).
- `status(unknown)` returns `None` (no raise); `control_mode(unknown)` returns `None`.
- `run()` returns int (distinct task ids dispatched).

## Known issues / open points

- Finding 3 (drain param) and 4 (footprint test) remain.
- The legacy `legio.manager` module functions (`submit`, `status`) and `api.py` are untouched (pinned `"local:"` behavior). Accepted debt (LEG-085 open item 1).

## Next steps

1. Commit this journal (rule 3) — done.
2. LEG-084 Registry implementation next.

---

# 2026-09-07 — Session 77 — Runtime LEG-084: Registry implementation (tests first)

## Scope

Implement LEG-084 (`legio.registry.Registry`) per approved contract. Tests first (red), then implementation (green).

## Work done (per issue)

- **LEG-084**: `tests/test_leg084_registry.py` written (contract tests); `src/legio/registry/__init__.py` implemented: `Registry(db)`, reads (`catalog`, `instances`, `yaml_cache`, `class_kind` for LEG-085/086), writes (`record_class`, `record_instance`, `remove_instance`, `remove_class`, `set_class_state`, `set_instance_state`, `cache_spec`, `get_cached_spec`). Fixed ordering: queue created → `record_class` + `cache_spec` (once) → per agent bring-up → `record_instance`. Idempotence: create existing → no-op; destroy non-existent → no-op; set_* on missing → error. Effective state reads (§4.4): stored enabled + ≥1 instance = enabled; else disabled. No layer opens another layer's scopes. Logging at all observable points.

## Tests run

- `uv run pytest tests/test_leg084_registry.py` — 24 passed.
- `uv run pytest -q` — 186 passed (no regressions).
- `uv run ruff check .` — All checks passed.
- `uv run pyright` — 0 errors.

## Decisions taken

- Registry is posterior mirror only: never initiates, never materializes, never runs anything. Governed only existence/lifecycle — never DAG/routing.
- `class_kind(name) -> AgentKind | None` added as read for LEG-085 lifecycle budgets.
- `get_cached_spec` returns the raw YAML string (for `recreate_class`).

## Known issues / open points

- Findings 3 (drain param) and 4 (footprint test) remain.
- LEG-085/086 still DRAFT, awaiting maintainer approval.

## Next steps

1. Commit this journal (rule 3) — done.
2. LEG-085 Runtime implementation next.

---

# 2026-09-07 — Session 78 — What a legio "contract" is and who reviews it; the coupling misgiving (analysis; no code)

## Scope

The maintainer asked who reviews the contracts and confessed unease: the word "contract" sounds like a bilateral agreement between two components, i.e. coupling. Clarified the meaning of `docs/CONTRACTS/` in legio. No code changed.

## The clarification (as given)

1. **A legio "contract" is a per-issue specification, not an inter-module agreement** — from contract-first TDD: before coding, write the "what it must do" and the tests that pin it. There are no "two parties"; there is ONE deliverable specified against its validation. The name is borrowed; the thing is "spec por issue".
2. **Who reviews it: the maintainer.** Contracts are written as DRAFT; per the process only the maintainer approves an issue (and only the maintainer closes it). Nothing under `docs/CONTRACTS/` is in force until approved.
3. **Contracts are ANTI-coupling, not coupling.** A contract pins the PUBLIC SURFACE of a single module (e.g. the Manager's API `submit_task`/`status`/`control_mode`/…). Consumers must reach through that documented surface, never into internals. Evidence: finding 2 — the Runtime reaching past the surface into the `control` scope *was* coupling, and the contract's "delegate via API" rule is what caught it. The contract draws the boundary; coupling is deleting it.
4. Recap of the catalog-slice status: contracts LEG-083/084/085/086 are DRAFT, awaiting maintainer approval; issues are provisional (#47-#50), held by the maintainer.

## Tests run

- None (analysis only). Tree unchanged.

## Known issues / open points

- Same as sessions 75-77: finding 3 decision pending (drain param naming/unit), finding 4 pending (footprint test). The maintainer needs to confirm the clarified contract concept before the R-8 slice is approved.

## Next steps

1. Commit this journal (rule 3) — done.
2. Await the maintainer: finding 3 (drain param) or finding 4 (footprint test), or a decision on the draft contracts.

---

# 2026-09-07 — Session 79 — DECOUPLING FIX: Runtime business records leave the Manager-owned `tasks` scope

## Scope

The maintainer rejected my "defer it" stance and ordered the coupling fixed now, recalling that every time he asks about coupling, a real one exists. Fixed the one remaining cross-layer scope write in the runtime triangle: the **Runtime was writing business flow records directly into the Manager-owned `tasks` scope** — prescribed by LEG-085:158 (my fault) and executed at runtime:90/235/272.

## Coupling inventory (post-session-76 state, now verified across src/)

| Layer | Own scopes opened | Touches another layer's scopes? |
|---|---|---|
| Manager | `tasks`, `pending_tasks`, `control` | no |
| Registry | `catalog`, `instances`, `yaml_cache` | no |
| Runtime | `gates`, `business_tasks` (**new**) | no — only class/result flow queues (queues are the decoupled message wiring shared by the flow, not a layer's private scope) |
| Agents (composite) | own `state:<id>` dict + gather queue | no |

Class/result queues (`legio:queue:<agent_id>`) are the flow's message medium, deposited by any submitter and polled by agents — by-design decoupling, not a scope.

## Work done

- **`src/legio/runtime/__init__.py`** — new Runtime-owned scope `_BUSINESS_TASKS_SCOPE = "business_tasks"`: `submit` stages the business record there (instead of Manager-owned `tasks`), `status` reads it there. Module docstring footprint section rewritten: the Runtime owns exactly `gates` + `business_tasks` and reaches the Manager **only through its public API** (`register`/`submit_task`/`status`/`control_mode`/`pause`/`resume`/`cancel`/`run`); it never opens another layer's scopes.
- **Tests** — `tests/test_leg085_runtime.py`: (a) two direct `control`-dict reads converted to the public `runtime.manager.control_mode(...)`; (b) business-record reads moved to `business_tasks`; (c) **two pinning tests**: business records never land in Manager `tasks`, Manager task records never land in Runtime `business_tasks`.
- **Contracts / docs** — LEG-085 (footprint paragraph, submit mechanics line, acceptance criteria: nobody writes another layer's scope, pinning tests); AGENT_LIFECYCLE (§6.1 Manager-owned-scopes note; the re-homed-submit paragraph — the Manager never sees business submissions; `business_tasks` is the Runtime's); LEG-083 (footprint + accept: scopes carry only generic task facts); PLAN.md R-8 LEG-085 accept updated to `gates` + `business_tasks` and reach-only-via-API.

## Tests run

- `uv run pytest tests/test_leg083_manager.py tests/test_leg085_runtime.py` — 56 passed.
- `uv run pytest -q` — 283 passed (281 + 2 pinning tests; no regressions).
- `uv run ruff check .` — All checks passed.
- `uv run pyright` — 0 errors, 0 warnings, 0 informations.

## Known issues / open points

- The coupling is removed from code *and* contracts; the R-8 slice (including this fix) stays uncommitted in the work tree, to be committed when the maintainer approves the slice. Findings 3 (drain param) and 4 (footprint test) remain; finding 4 is now largely absorbed by the two pinning tests (a broader scope-footprint test could still be desirable).

## Next steps

1. Commit this journal (rule 3) — done.
2. Report the fix to the maintainer; on his call, finding 3 (drain) and the R-8 slice approval.

---

# 2026-09-07 — Session 80 — DECOUPLING REWORK: Business submit/status rides the Manager `seed` task; Runtime owns only `gates`; no pump; lifecycle budgets from config

## Scope

The maintainer rejected the session-79 `business_tasks` scope approach and approved a 6-point decoupling proposal ("hazlo"): **the business `submit`/`status` mounts on the Manager as a `seed` task** — the Manager is the single task substrate; the Runtime owns **only `gates`** on beaver; the Runtime **does not pump** (the node drives the executor via `manager.run()`); lifecycle verbs use **bounded clock waits over the lifecycle budgets** (§5.8/§10.2) from `LifecycleConfig` (LEG-081); the one-shot bring-up fact is a terminal SUCCESS callable; `destroy_instance` confirms a terminal state (SUCCESS for a replayed cancel, `failed(cancelled)` for a live one); `business_tasks` scope is removed; `_CONFIRM_PASSES` and `drain_interval_ms` are removed.

## Work done (per issue)

- **LEG-081 config** (`src/legio/config.py`): added `_BUILTIN_DRAIN_TIMEOUT = 300.0`, `_BUILTIN_DRAIN_INTERVAL = 0.05`; `LifecycleParams(drain_timeout: float|None, drain_interval: float|None)` with `gt(0)` validator; `LifecycleConfig(per_pattern, per_kind, default)` with `resolve(pattern, per_kind)` implementing overlay precedence (builtin < default < per_kind < per_pattern); `LifecycleConfig` field added to `LegioConfig`; exports updated.
- **LEG-083 Manager** (`src/legio/manager/__init__.py`): `submit_task(name, *args, task_id=None, **kwargs)` — mints `<node_id>:<uuid>` when `task_id` is `None`, validates explicit `task_id` via `legio.naming.validate_task_id`, rejects existing `task_id` with visible `ValueError`. Import `validate_task_id` added.
- **LEG-084 Registry** (`src/legio/registry/__init__.py`): `class_kind(name) -> AgentKind | None` read added.
- **LEG-085 Runtime** (`src/legio/runtime/__init__.py`): **fully rewritten** — new constructor `Runtime(db, *, node_id, manager=None, registry=None, lifecycle=None)`; registers `BRING_UP_TASK = "bring_up"` and `SEED_TASK = "seed"`; `_bring_up_impl` (one-shot, returns `instance_id`); `_seed_impl(client_id, token, payload)` validates root token, deposits `ExecutionRequestMessage` to `queue_key(token.launcher_class)`, returns token dump; `_lifecycle_budget(class_name)` resolves via `registry.class_kind`; `_await_observable_state` / `_confirm_control` (bounded clock wait over lifecycle budgets); `submit` (gate check → mint `task_id` → `FlowToken` → `manager.submit_task(SEED_TASK, task_id=..., client_id=..., token=..., payload=...)`); `status` (owner via `kwargs["client_id"]`, FAILED → `RecoverableError`, peek final-result queue → COMPLETED); lifecycle verbs (class: `create_class`/`recreate_class`/`create_from_catalog`/`enable_class`/`disable_class`/`destroy_class`; instance: `create_instance`/`enable_instance`/`disable_instance`/`destroy_instance`) — all action → fact → confirm → record; `destroy_class(mode="drain"|"now")` with lifecycle budgets, `_await_queue_empty` restores gate on timeout; read delegations incl. `class_kind`; **removed**: `business_tasks`, `_BUSINESS_TASKS_SCOPE`, `_bring_up_gates`, `_CONFIRM_PASSES`, `_drive_until`, pump, `run`/`register` passthroughs, gate events, puppet.
- **Tests**: `tests/test_leg081_config.py` extended with lifecycle section (per_pattern/per_kind/default, built-in fallback, overlay, fail-fast non-positive/unknown kind) — 35 passed; `tests/test_leg085_runtime.py` **fully rewritten** for the new contract: node pump `_start_executor` drives `manager.run()`, `_teardown` with pump param, `_expect_seed_success` for determinism; pins inverted — business submit lands in Manager `tasks` as `seed` (no `business_tasks` scope); `test_node_drives_the_executor_through_the_manager` verifies Runtime has no `run`/`register`; `tests/test_leg086_boot_catalog.py` pump added to all tests creating classes.
- **Contracts/docs**: LEG-085 rewritten to match the new design; LEG-083 footprint updated (business seed is a Manager task minted via `submit_task`); AGENT_LIFECYCLE §6 invariant + §6.1 submit paragraph + beaver footprint note updated; PLAN.md R-8 LEG-085 accept updated (Runtime owns only `gates`, Manager holds seed, no Runtime pump). All citations harmonized to repo convention (§5.8/§10.2 for drain-wait exception, ARCHITECTURE §7 for seed).

## Logging added

- `legio.runtime` module logger (`logging.getLogger(__name__)`) emits structured events at every verb: INFO on class/instance created/destroyed/state-changed, on submit/gate open/close, DEBUG on confirm polls/drain polls, WARNING on no-ops/denials, `logger.exception` never for expected rejections (rule 11). Logging shipped with implementation.

## Tests run

- `uv run pytest tests/test_leg081_config.py` — 35 passed.
- `uv run pytest tests/test_leg085_runtime.py tests/test_leg086_boot_catalog.py` — 42 passed.
- `uv run pytest -q` — 291 passed (full suite).
- `uv run ruff check .` — All checks passed.
- `uv run pyright` — 0 errors, 0 warnings, 0 informations.

## Decisions taken

- **Business submit IS the Manager's `seed` task** — record lives in Manager `tasks` (created via `submit_task` with extra-official kwargs `client_id`/`token`/`payload`); the Manager stays blind (rule 7). No `business_tasks` scope.
- **One-shot bring-up** — `_bring_up_impl` returns `instance_id` immediately (terminal SUCCESS); no parked generator, no instance gate, no `_bring_up_gates`. A boot overwrites this callable at the same seam.
- **Bounded clock waits use lifecycle config** — `_await_observable_state` / `_confirm_control` poll `manager.status` / `control_mode` every `drain_interval` up to `drain_timeout` (resolved per class via `LifecycleConfig.resolve`); FAILED record that predicate rejects raises immediately (rule 9); budget expiry raises visible `RecoverableError` with last state. No `_CONFIRM_PASSES`, no `drain_interval_ms`.
- **Node owns executor** — Runtime exposes `manager` attribute; the node (and tests) drives `manager.run()`. Runtime has no `run()`/`register()`.
- **`destroy_instance` confirms terminal** — the one-shot bring-up is already SUCCESS; a cancel reaching a completed record is a terminal no-op; a pending cancel lands `failed(cancelled)`. Either is the confirmed fact.
- **`drain`/`now` destroy** — `drain` closes gate, waits queue empty (bounded by lifecycle budgets); on timeout → visible error, gate restored. `now` proceeds immediately.
- **Legacy `legio.manager.submit/status` and `api.py` untouched** — pinned `"local:"` behavior preserved; unified when `api.py` re-targets to Runtime (LEG-080/081).
- **`recreate_class` reads cached YAML** — precondition: class does not exist; cache miss → visible `RecoverableError`.

## Known issues / open points

- Contracts LEG-083/084/085/086, AGENT_LIFECYCLE, PLAN.md updated in this session but **uncommitted** (rule 3: only the journal is committed per turn; work tree waits for maintainer review/approval).
- The R-8 slice (LEG-081/083/084/085/086) is complete and tested, pending maintainer approval to commit the work tree.

## Next steps

1. Commit this journal (rule 3) — done.
2. Await maintainer review of the R-8 slice (code + contracts + docs); on approval, commit the work tree.

---

# 2026-09-07 — Session 81 — AUDIT: code vs design docs (subagent), full audit report recorded verbatim

## Scope

The maintainer requested an audit of the code against the design documents. A subagent (`explore`) performed a thorough audit against AGENTS.md rules, ARCHITECTURE.md, AGENT_LIFECYCLE.md, PLAN.md, and CONTRACTS/LEG-083/084/085/086. The following is the subagent's report copied **exactly as produced** (verbatim), so tomorrow's session resumes from this point.

## The audit report (verbatim)

---

# AUDIT REPORT: legio Codebase vs Design Documents

## Executive Summary

The codebase passes all 291 tests, ruff linting, and pyright type checking. However, **multiple deviations from the design documents and contracts exist** across AGENTS.md rules, ARCHITECTURE.md, AGENT_LIFECYCLE.md, and the LEG-083/084/085/086 contracts. The most recent journal (Session 80) documents a major rework that addressed some coupling issues but introduced new deviations.

---

## 1. AGENTS.md Rule Compliance

### Rule 3: Journal Committed Every Turn
- **Status**: ✅ **COMPLIANT** — Journal entries exist for 2026-09-07 (Sessions 75-80) and are committed.

### Rule 7: Domain-Free Library
**DEVIATIONS FOUND:**

| Location | Issue |
|----------|-------|
| `src/legio/manager/__init__.py:47-48` | `_DEFAULT_NODE_ID = "local"` — hardcoded `"local"` node_id used as default; violates rule 7 (domain-free) as this leaks a default consumer identity. The Manager constructor validates `node_id` via `validate_node_id` which rejects `"local"` (must be `<name>@<host>`). The legacy `submit`/`status` functions use `_node_id = "local"` globally. |
| `src/legio/manager/__init__.py:462-525` | Legacy `submit`/`status` functions use `"local:"` prefix for task_ids — this is a consumer-domain leakage. The tests pin this behavior (`tests/test_leg025_worker.py`, `tests/test_leg027_auth.py`). |
| `src/legio/materializer.py:338-356` | `NodeRuntime.app` property exposes `starting_agents` derived from `catalog.specs[spec.main]` — this is correct (eligibility flag), but the HTTP surface in `api.py` uses this directly. |

### Rule 8: Polling Only — Nothing Sleeps
**DEVIATIONS FOUND:**

| Location | Issue |
|----------|-------|
| `src/legio/agents/base.py:120-132` | `AgentBase.run(max_steps=100)` uses a hardcoded default `max_steps=100` — this bounds the polling loop but is a **configurable parameter that should come from lifecycle config**, not a literal. AGENT_LIFECYCLE §12.2 says "Bounded so a misbehaving step can never starve the agent into an infinite busy loop" but the bound should be configurable. |
| `src/legio/runtime/__init__.py:184, 207` | `_await_observable_state` and `_confirm_control` use `await asyncio.sleep(interval)` — this is the **documented exception** per AGENT_LIFECYCLE §5.8/§10.2 (bounded clock waits over lifecycle budgets). ✅ **Documented exception, compliant** but note: the interval comes from `LifecycleConfig` (good). |
| `src/legio/runtime/__init__.py:569-579` | `_await_queue_empty` uses `await asyncio.sleep(interval)` — same documented exception. ✅ **Compliant**. |

### Rule 9: Errors Never Silent
**DEVIATIONS FOUND:**

| Location | Issue |
|----------|-------|
| `src/legio/agents/base.py:170-176` | `_process_inbox_item` catches `Exception`, logs with `logger.exception`, then **re-raises**. This crashes the agent loop — the error is visible in logs but the agent dies. Per AGENTS.md rule 9: "Failures are visible in registries and in the task result. Never swallow exceptions." The agent crashing is visible but the **task result is not recorded** — the message is lost. Per ARCHITECTURE §8: "a step that raises is routed to a visible error result; the task ends in a known terminal state." The base agent's `_run_guarded` (line 179-208) handles this correctly for step errors, but `_process_inbox_item` crashing the whole loop is a gap. |
| `src/legio/manager/__init__.py:298-308` | `_await_plain` catches `Exception`, records `FAILED` with error, logs with `logger.exception`. ✅ **Compliant**. |
| `src/legio/runtime/__init__.py:181-183` | `_await_observable_state` raises `RecoverableError` immediately on `FAILED` record. ✅ **Compliant**. |

### Rule 11: Logging at Every Observable Point
**DEVIATIONS FOUND:**

| Location | Issue |
|----------|-------|
| `src/legio/manager/__init__.py:148` | `logger.info("manager executor up node=%s scopes=tasks,pending_tasks,control", node_id)` — ✅ Good. |
| `src/legio/manager/__init__.py:187` | `logger.info("manager submit task=%s name=%s", task_id, name)` — ✅ Good. |
| `src/legio/manager/__init__.py:210, 218, 226` | `pause`/`resume`/`cancel` log at INFO. ✅ Good. |
| `src/legio/manager/__init__.py:284, 308, 314, 331, 346, 363` | Various DEBUG/INFO/WARNING/exception logs. ✅ Good. |
| `src/legio/registry/__init__.py:131-138, 143, 180-185, 193, 211-216, 230-234, 249` | All operations log at INFO, no-ops at WARNING, errors at ERROR. ✅ **Compliant**. |
| `src/legio/runtime/__init__.py:106, 146-152, 231-237, 283-289, 371-377, 404-405, 441-445, 514-515, 531-532, 561, 580, 603-608, 628-629, 648-649, 684, 793, 796, 799` | Comprehensive structured logging at every verb. ✅ **Compliant**. |
| `src/legio/agents/base.py:161-167, 197-202, 295-301, 316-340` | Agent loop logs at INFO/DEBUG/WARNING. ✅ **Compliant**. |
| `src/legio/agents/composite_agent.py:162-167, 215-224, 268-290` | Composite fan-out/fan-in logs. ✅ **Compliant**. |
| `src/legio/agents/tool_agent.py:72-77, 82-88` | Tool execution logs. ✅ **Compliant**. |
| `src/legio/agents/linguistic_agent.py:73-78, 80-91` | LLM call logs. ✅ **Compliant**. |

**Overall**: Rule 11 logging is **largely compliant** and shipped with implementation.

### Rule 12: Implement to Design, Re-verify
**DEVIATIONS FOUND** — Multiple deviations from ARCHITECTURE.md and contracts documented below.

---

## 2. ARCHITECTURE.md Compliance

### Runtime Triangle: Manager / Registry / Runtime
| Layer | Design (ARCHITECTURE.md) | Implementation | Status |
|-------|--------------------------|----------------|--------|
| **Manager** | Generic task env, blind to domain, owns `tasks`/`pending_tasks`/`control` | ✅ `src/legio/manager/__init__.py` implements `Manager` with these scopes | **COMPLIANT** |
| **Registry** | Posterior mirror, owns `catalog`/`instances`/`yaml_cache`, never initiates | ✅ `src/legio/registry/__init__.py` implements `Registry` with these scopes | **COMPLIANT** |
| **Runtime** | Owns only `gates`, public face, orchestrates Manager+Registry | ⚠️ **See deviations below** | **PARTIAL** |

### Runtime Deviations from ARCHITECTURE.md

| ARCHITECTURE Section | Design Requirement | Implementation | Deviation |
|----------------------|-------------------|----------------|-----------|
| §2 (Substrate) | "Registry — beaver persistent dict per scope: the mirror Registry's scopes — the live catalog and the runtime YAML cache... Future scopes: `gates` (Runtime-written class gate, read by depositors — AGENT_LIFECYCLE §12.5)" | `src/legio/runtime/__init__.py:102` creates `self._gates = db.dict("gates")` | ✅ **COMPLIANT** |
| §2 (Substrate) | "There is **no** `results` return store: the final result is delivered to the **final-result queue**" | `Runtime.status` reads from `result_queue_key(task_id)` via `peek` | ✅ **COMPLIANT** |
| §7 (Task Lifecycle) | "Client calls the API → **Runtime**... does the **Manager**'s `submit_task` — the task the Manager runs seeds the flow" | `Runtime.submit` calls `manager.submit_task(SEED_TASK, ...)` | ✅ **COMPLIANT** |
| §7 Step 2 | "The **submit** seeds the flow: `ExecutionRequestMessage` on the `main` class in **level 1**, `end_of_level_queue` = the **final-result queue**" | `Runtime.submit` builds `FlowToken` with `end_of_level_queue=result_queue` and `level=1` | ✅ **COMPLIANT** |
| §7 Step 7 | "The client polls `status(task_id)` → Runtime reads the final result from the per-task final-result queue" | `Runtime.status` peeks `result_queue_key(task_id)` | ✅ **COMPLIANT** |
| §12.5 (Gate) | "One registry `db.dict("gates")`, keyed by class name = `{state: enabled\|disabled}`. **Written only by the Runtime**" | `Runtime` writes `gates` in `create_class`, `enable_class`, `disable_class`, `destroy_class` | ✅ **COMPLIANT** |
| §12.5.3 | "Row absent = open" | `Runtime.submit` treats missing gate as open (line 258-261) | ✅ **COMPLIANT** |
| §12.5.3 | "destroyed class: queue cleared + gate row removed, every deposit blocked at the missing gate" | `destroy_class` calls `_clear_queue` and `_gates.delete(name)` (lines 553-554) | ✅ **COMPLIANT** |
| §12.5.5 | "Exemptions: an error-result deposit always proceeds regardless of the gate" | **NOT IMPLEMENTED** — no exemption logic in deposit paths | ❌ **DEVIATION** |
| §13 (Dependencies) | "beaver-db, lingo-ai, pydantic, fastapi+uvicorn, httpx, pyyaml, typer, pytest..." | All present in `pyproject.toml` | ✅ **COMPLIANT** |

### Layers Never Write Another Layer's Scopes
| Design | Implementation | Status |
|--------|----------------|--------|
| Manager owns `tasks`/`pending_tasks`/`control` | Only Manager writes these | ✅ |
| Registry owns `catalog`/`instances`/`yaml_cache` | Only Registry writes these | ✅ |
| Runtime owns `gates` | Only Runtime writes `gates` | ✅ |
| **Business submit** | **Session 80: "business submit IS the Manager's `seed` task — record lives in Manager `tasks` (created via `submit_task` with extra-official kwargs)"** | `Runtime.submit` calls `manager.submit_task(SEED_TASK, task_id=..., client_id=..., token=..., payload=...)` | ✅ **COMPLIANT** (per Session 80 rework) |
| **Legacy `legio.manager.submit`/`status`** | Still use `task_registry()` (Manager's `tasks` scope) directly | `src/legio/manager/__init__.py:496-505` writes to `tasks` dict; `api.py` uses this | ⚠️ **KNOWN DEVIATION** (legacy, pinned `"local:"` behavior, documented in LEG-085 open item 1) |

### Node Owns Executor (Runtime Never Pumps)
| Design | Implementation | Status |
|--------|----------------|--------|
| "The **node owns the executor** — the boot runs `manager.run()` in its own loop... The Runtime never owns an executor" (LEG-085) | `Runtime` has no `run()`/`register()`; exposes `self.manager`; tests drive pump via `_start_executor` | ✅ **COMPLIANT** |
| "One-shot bring-up, no parked generator" (AGENT_LIFECYCLE §5.1/§5.2, LEG-085) | `_bring_up_impl` returns `instance_id` immediately (terminal SUCCESS) | ✅ **COMPLIANT** |

---

## 3. LEG-083 (Manager) Contract Compliance

| Contract Requirement | Implementation | Status |
|---------------------|----------------|--------|
| `submit_task(name, *args, task_id=None, **kwargs) -> task_id` | ✅ Implemented (line 159-188) | **COMPLIANT** |
| `task_id` minted as `<node_id>:<uuid>` when `None` | ✅ Line 170-171 | **COMPLIANT** |
| Explicit `task_id` validated via `validate_task_id` | ✅ Line 172-173 | **COMPLIANT** |
| Reuse of existing `task_id` raises `ValueError` | ✅ Line 174-176 | **COMPLIANT** |
| `status(task_id) -> TaskRecord \| None` | ✅ Line 190-195 | **COMPLIANT** |
| `control_mode(task_id) -> str \| None` | ✅ Line 197-204 | **COMPLIANT** |
| `pause`/`resume`/`cancel` | ✅ Lines 206-226 | **COMPLIANT** |
| `run()` polls once, non-blocking, returns count | ✅ Lines 228-241 | **COMPLIANT** |
| Beaver scopes exactly `tasks`/`pending_tasks`/`control` | ✅ Lines 145-147 | **COMPLIANT** |
| No result queue, no scheduling queue, no `next_run_at` | ✅ Confirmed | **COMPLIANT** |
| Callable registry in-process | ✅ Line 142 | **COMPLIANT** |
| Task executor algorithm per spec | ✅ Lines 243-364 | **COMPLIANT** |
| Logging at all observable points | ✅ | **COMPLIANT** |

**LEG-083: FULLY COMPLIANT**

---

## 4. LEG-084 (Registry) Contract Compliance

| Contract Requirement | Implementation | Status |
|---------------------|----------------|--------|
| Beaver scopes: `catalog`/`instances`/`yaml_cache` | ✅ Lines 97-99 | **COMPLIANT** |
| `record_class` + `cache_spec` (posteriori, once) | ✅ Lines 102-143 | **COMPLIANT** |
| `record_instance` after agent running | ✅ Lines 145-185 | **COMPLIANT** |
| `record_instance` on unknown class → error | ✅ Line 157-164 | **COMPLIANT** |
| `set_class_state`/`set_instance_state` on unknown → error | ✅ Lines 187-216 | **COMPLIANT** |
| `remove_instance`/`remove_class` idempotent | ✅ Lines 218-249 | **COMPLIANT** |
| `instance_id` is agent's identity, never task_id | ✅ Line 63-65, 68-69 | **COMPLIANT** |
| Ordering: queue → record_class → record_instance | Enforced by Runtime, Registry validates | ✅ **COMPLIANT** |
| Effective state: stored enabled + ≥1 instance | ✅ `_effective_class_state` (lines 349-359) | **COMPLIANT** |
| Queries: all specified queries implemented | ✅ Lines 251-342 | **COMPLIANT** |
| Kind binding: `AgentKind` or `None` (composite) | ✅ Lines 116-118 | **COMPLIANT** |
| Logging at all observable points | ✅ | **COMPLIANT** |

**LEG-084: FULLY COMPLIANT**

---

## 5. LEG-085 (Runtime) Contract Compliance

### Major Deviations from LEG-085 Contract

| Contract Section | Requirement | Implementation | Deviation |
|------------------|-------------|----------------|-----------|
| **Construction** | `Runtime(db, *, node_id, manager=None, registry=None, lifecycle=None)` | ✅ Line 75-106 | **COMPLIANT** |
| **Constants** | `BRING_UP_TASK = "bring_up"`, `SEED_TASK = "seed"` | ✅ Lines 62-63 | **COMPLIANT** |
| **One-shot bring-up** | "not a parked generator... The boot materializing real agents **overwrites this callable**" | ✅ `_bring_up_impl` returns immediately | **COMPLIANT** |
| **Seed task** | Validates root token, deposits root message, returns token dump | ✅ Lines 120-153 | **COMPLIANT** |
| **Lifecycle budgets** | `LifecycleConfig.resolve(class_name, per_kind=kind)` | ✅ Lines 157-161 | **COMPLIANT** |
| **Bounded clock waits** | `_await_observable_state` / `_confirm_control` poll with timeout/interval from config | ✅ Lines 163-212 | **COMPLIANT** |
| **Gate check in submit** | Check `gates` for starting class before minting | ✅ Lines 257-261 | **COMPLIANT** |
| **Submit mints task_id** | `task_id = f"{self._node_id}:{uuid.uuid4()}"` | ✅ Line 262 | **COMPLIANT** |
| **Re-key payload** | `rekeyed_payload = {first_input_as: payload}` | ✅ Line 264 | **COMPLIANT** |
| **Root branch_id** | `root_branch_id = str(uuid.uuid4())` | ✅ Line 265 | **COMPLIANT** |
| **Seed task kwargs** | `client_id`, `token`, `payload` as extra-official kwargs | ✅ Lines 276-282 | **COMPLIANT** |
| **Status owner check** | `record.kwargs.get("client_id") != client_id` → `PermissionError` | ✅ Lines 303-310 | **COMPLIANT** |
| **Status reads final-result queue** | `peek` on `result_queue_key(task_id)` | ✅ Lines 317-323 | **COMPLIANT** |
| **Gate written at class birth** | `gates[name] = {state: disabled}` in `create_class` | ✅ Line 352 | **COMPLIANT** |
| **Record class + cache_spec** | Before pool bring-up | ✅ Lines 353-361 | **COMPLIANT** |
| **Born enabled iff pool>0 AND deps satisfied** | ✅ Line 362 | **COMPLIANT** |
| **Instances born disabled if class disabled** | ✅ Lines 363-367 | **COMPLIANT** |
| **Enable class brings up one instance if none** | ✅ Lines 501-502 | **COMPLIANT** |
| **Disable class: gate first, then cascade** | ✅ Lines 526-531 | **COMPLIANT** |
| **Destroy class drain: bounded wait with gate restore on timeout** | ✅ Lines 545-580 | **COMPLIANT** |
| **Destroy class removes gate row** | ✅ Line 554 | **COMPLIANT** |
| **Destroy class cascade dependents (not dependencies)** | ✅ Lines 555-560 | **COMPLIANT** |
| **Recreate from cached spec** | ✅ Lines 379-405 | **COMPLIANT** |
| **Create from catalog (LEG-086)** | ✅ Lines 407-445 | **COMPLIANT** |
| **No `run()`/`register()` on Runtime** | ✅ Lines 403-406 | **COMPLIANT** |

### LEG-085: SUBSTANTIALLY COMPLIANT with minor issues below

---

## 6. LEG-086 (Boot Catalog) Contract Compliance

| Contract Requirement | Implementation | Status |
|---------------------|----------------|--------|
| Topological order (leaves first, Kahn's algorithm) | ✅ `_catalog_order` (lines 447-481) | **COMPLIANT** |
| Cycle detection before any record | ✅ Lines 472-477 | **COMPLIANT** |
| Pool resolution: override > per_pattern > per_kind > default > 1 | ✅ `_resolve_pool` (lines 483-491) | **COMPLIANT** |
| Spec_yamls feeds cache | ✅ Lines 432, 436-439 | **COMPLIANT** |
| Unsatisfied deps → born disabled | ✅ Via `create_class` logic | **COMPLIANT** |
| Intent vs real count recorded | ✅ `create_class` records actual instances | **COMPLIANT** |

**LEG-086: FULLY COMPLIANT**

---

## 7. AGENT_LIFECYCLE.md Compliance

### §4.6 Destroy Parameter: `now` / `drain`
| Design | Implementation | Status |
|--------|----------------|--------|
| `drain` default, waits for queue empty | ✅ `destroy_class(mode="drain")` default | **COMPLIANT** |
| Large timeout guards wait (human-scale) | ✅ `LifecycleConfig` provides `drain_timeout`/`drain_interval` | **COMPLIANT** |
| On expiry: visible failure, class left untouched | ✅ `_await_queue_empty` restores gate, raises `RecoverableError` | **COMPLIANT** |
| `now`: proceed immediately, accept loss | ✅ Line 547-548 | **COMPLIANT** |

### §5.1-§5.9 Lifecycle Verb Flows
All verb flows implemented per the translate table (§6.1). **COMPLIANT**

### §6.1 Responsibilities + Submit Flow
| Design | Implementation | Status |
|--------|----------------|--------|
| Runtime decides, Manager executes, Registry records | ✅ All verbs follow action→fact→confirm→record | **COMPLIANT** |
| Confirmation is Manager read (`status`/`control_mode`) | ✅ `_await_observable_state` / `_confirm_control` | **COMPLIANT** |
| Business submit = Manager `seed` task | ✅ `Runtime.submit` → `manager.submit_task(SEED_TASK, ...)` | **COMPLIANT** |
| Manager blind to domain | ✅ Manager never inspects seed kwargs | **COMPLIANT** |

### §10.2 / §5.8 Clock-Wait Exception
| Design | Implementation | Status |
|--------|----------------|--------|
| Bounded clock waits use lifecycle budgets | ✅ `_lifecycle_budget` resolves from `LifecycleConfig` | **COMPLIANT** |
| No `_CONFIRM_PASSES`, no `drain_interval_ms` | ✅ Removed in Session 80 | **COMPLIANT** |

---

## 8. Code Quality Issues

### Semantic Naming (AGENTS.md Rule 2)
| Location | Issue | Severity |
|----------|-------|----------|
| `src/legio/manager/__init__.py:47-48` | `_DEFAULT_NODE_ID = "local"` — `local` is not a valid node_id per naming contract (`<name>@<host>`); misleading name | Medium |
| `src/legio/manager/__init__.py:50-52` | Module globals `_db`, `_db_path`, `_node_id` — mutable global state, anti-pattern | Medium |
| `src/legio/runtime/__init__.py:219` | `born_state` variable — could be `effective_state` or `initial_instance_state` | Low |
| `src/legio/agents/base.py:120` | `max_steps=100` — magic number, should be configurable | Medium |

### Hardcoded Values Where Config Should Be Used
| Location | Hardcoded Value | Should Be Config |
|----------|----------------|------------------|
| `src/legio/agents/base.py:120` | `max_steps: int = 100` | Agent loop bound should come from lifecycle/node config |
| `src/legio/manager/__init__.py:47` | `_DEFAULT_NODE_ID = "local"` | Node ID must come from config (already validated in constructor) |
| `src/legio/manager/__init__.py:48` | `_DEFAULT_DB_PATH = "legio.db"` | DB path from config (already handled by `connect_manager`) |

### Inconsistent Citations
| Location | Citation | Issue |
|----------|----------|-------|
| `src/legio/runtime/__init__.py:34-35` | "§5.8/§10.2, rule 8 exception" | References AGENT_LIFECYCLE sections correctly |
| `src/legio/runtime/__init__.py:563-564` | "rule 8 exception documented in §5.8/§10.2" | Consistent |
| `src/legio/config.py:129-133` | "rule 8 exceptions, §5.8/§10.2" | Consistent |
| `src/legio/manager/__init__.py:8-11` | "Schema 2" references | Legacy mini-manager uses different schema refs |

---

## 9. Specific File-Level Deviations (Requested Files)

### `src/legio/runtime/__init__.py`
| Line | Deviation |
|------|-----------|
| 102 | `self._gates = db.dict("gates")` — Correct per ARCHITECTURE §2, but AGENT_LIFECYCLE §12.5.2 says "gates" scope is `db.dict("gates")` — ✅ **Correct** |
| 103 | `self._instance_tasks: dict[tuple[str, str], str] = {}` — In-process map, not persisted. Per LEG-085 this is correct. But on restart, `_instance_tasks` is lost and `destroy_instance` raises `RecoverableError` (lines 661-672). This is a **known open item** (LEG-085 open item 2). |
| 159 | `_lifecycle_budget` calls `self.registry.class_kind(class_name)` — but `class_kind` returns `None` for unknown class AND for composites. For composites, `per_kind=None` is passed to `LifecycleConfig.resolve`, which falls back to `per_pattern`/`default`. This is correct per config design. |
| 352 | `await self._gates.set(name, {"state": ActivityState.DISABLED.value})` — Gate written at birth (disabled). ✅ Correct. |
| 487 | `validate_task_id` called in Manager, but Runtime mints its own `task_id` via `f"{self._node_id}:{uuid.uuid4()}"` — Runtime doesn't validate its own minted IDs. Should call `validate_task_id` for consistency. |
| 517-518 | `destroy_instance` confirms terminal state via `_await_observable_state` with `ok=lambda record: record.status in (TaskStatus.SUCCESS, TaskStatus.FAILED)`. Per LEG-085: "the one-shot bring-up fact is already SUCCESS, and a cancel reaching a completed record is a terminal no-op". But the predicate accepts `SUCCESS` which is wrong — a replayed cancel on a completed task should be a no-op, but `SUCCESS` means the bring-up succeeded, not that the cancel was processed. This is a **logic gap**. |

### `src/legio/manager/__init__.py`
| Line | Deviation |
|------|-----------|
| 47-48 | Module globals `_DEFAULT_NODE_ID = "local"`, `_DEFAULT_DB_PATH = "legio.db"` — violates domain-free (Rule 7). The `"local"` node_id is rejected by `validate_node_id`. |
| 50-52 | Global mutable state `_db`, `_db_path`, `_node_id` — not thread-safe, not test-isolated without `reset_manager`. |
| 135 | `control_ttl: float = 60.0` — hardcoded TTL for control dict. Should be configurable via `LifecycleConfig` or separate config. |
| 159-188 | `submit_task` accepts `task_id` parameter but legacy `submit` function (line 461) doesn't use it — inconsistency. |
| 461-525 | Legacy `submit`/`status` functions duplicate logic with `Runtime.submit`/`status` but use `"local:"` task_id prefix and `task_registry()` directly. **Known deviation** (LEG-085 open item 1). |
| 528-566 | Legacy `status` reads from `task_registry()` (Manager's `tasks` scope) not from Runtime's business surface. |

### `src/legio/registry/__init__.py`
| Line | Deviation |
|------|-----------|
| 256-258 | `class_state` calls `_effective_class_state` which iterates all instance keys to check if any exist. This is O(n) per call. Could be optimized with a counter, but acceptable for current scale. |
| 284-311 | `class_dependents` with `transitive=True` does nested iteration over all catalog values — O(n²) worst case. Acceptable for small catalogs. |
| 349-359 | `_effective_class_state` reads catalog then scans instances — could race if instance added between reads. But Registry is posterior mirror, so this is read-only and acceptable. |

### `src/legio/config.py`
| Line | Deviation |
|------|-----------|
| 122-123 | `_BUILTIN_DRAIN_TIMEOUT = 300.0`, `_BUILTIN_DRAIN_INTERVAL = 0.05` — module-level constants, should be class constants on `LifecycleParams` or `LifecycleConfig`. |
| 136-137 | `drain_timeout: float \| None = None`, `drain_interval: float \| None = None` — optional fields with `None` default. The validator ensures `> 0` when set. But the built-in defaults are applied in `LifecycleConfig.resolve` (line 168-171). This is correct but the constants are duplicated. |
| 39-45 | `SECRET_ENV_NAMES` and `CLIENT_TOKEN_PREFIX` — hardcoded env var names. Acceptable as they're the contract. |

### `tests/test_leg085_runtime.py`
| Line | Deviation |
|------|-----------|
| 146-154 | `_expect_seed_success` polls 200 times with 0.01s sleep = 2s max wait. This is a **busy-wait in tests** but tests are allowed this for determinism. |
| 345-361 | `test_disable_class_policy_a_instances_keep_draining` — verifies instance state stays ENABLED when class disabled. But per AGENT_LIFECYCLE §4.1, instances of a disabled class "keep processing the pending work until drained" — they are not paused. The test correctly checks instance state is ENABLED. ✅ |
| 364-396 | `test_disable_class_cascades_dependents_transitively` — creates 3 classes, verifies all 3 disabled. ✅ |
| 399-419 | `test_cascade_is_not_auto_undone_by_enabling_the_dependency` — re-enabling dependency doesn't auto-re-enable dependents. ✅ Per AGENT_LIFECYCLE §7. |
| 492-514 | `test_destroy_class_now_clears_queue_gate_and_class` — submits a task, then destroys class with `now`, verifies queue cleared, gate removed, class removed, YAML cached. ✅ |
| 517-541 | `test_destroy_class_drain_waits_for_queue_to_empty` — verifies drain waits for queue to empty. Uses `asyncio.sleep(0.05)` to let drain start, then manually clears queue. ✅ |
| 544-567 | `test_destroy_class_drain_times_out_visibly_and_leaves_class` — uses `LifecycleConfig(default=LifecycleParams(drain_timeout=0.2, drain_interval=0.05))` for fast test. ✅ |
| 638-779 | Business submit/status tests — use `ToolAgent` with fake tool to drive flow to completion. Tests the full integration. ✅ |

### `tests/test_leg086_boot_catalog.py`
| Line | Deviation |
|------|-----------|
| 139-163 | `test_boot_orders_leaves_first_so_dependents_born_enabled` — creates catalog with 3 classes, verifies all born enabled. ✅ |
| 166-240 | Pool resolution tests — verify precedence correctly. ✅ |
| 243-255 | `test_boot_pool_zero_born_disabled` — verifies pool=0 → disabled, no instances. ✅ |
| 258-269 | `test_boot_caches_each_spec_yaml` — verifies YAML cached. ✅ |
| 275-290 | `test_boot_rejects_dependency_cycle_before_anything_recorded` — cycle raises, `list_classes()` empty. ✅ |
| 293-306 | `test_boot_skips_unsatisfied_dependency_but_records_disabled` — composite with missing dep created disabled. ✅ |
| 309-321 | `test_boot_unknown_spec_in_yamls_is_ignored` — extra YAML entries ignored. ✅ |

### `tests/test_leg081_config.py`
All 35 tests pass. Tests cover defaults, file loading, precedence, pools, lifecycle, fail-fast, secrets, tools file. **COMPLIANT**.

---

## 10. Cross-Cutting Concerns

### FlowToken / Message Compliance (Schema 2, AGENT_LIFECYCLE §4.11)
| Field | Design | Implementation | Status |
|-------|--------|----------------|--------|
| `schema_version` = 1000 | ✅ `SCHEMA_VERSION = 1000` in messages.py | **COMPLIANT** |
| `level_route` tuple of `(class, input_as)` | ✅ `RouteStep = tuple[str, str]` | **COMPLIANT** |
| `current_index` 0-based | ✅ | **COMPLIANT** |
| `end_of_level_queue` | ✅ | **COMPLIANT** |
| `level` starts at 1 | ✅ | **COMPLIANT** |
| `launcher_class` constant | ✅ | **COMPLIANT** |
| `task_id` string | ✅ | **COMPLIANT** |
| `branch_id` autogenerated uuid4 | ✅ `str(uuid.uuid4())` in Runtime.submit and CompositeAgent._fan_out | **COMPLIANT** |
| `message_type` enum | ✅ `MessageType.EXECUTION_REQUEST/RESULT` | **COMPLIANT** |
| `payload` single container | ✅ | **COMPLIANT** |
| `root` flag on FlowToken | ✅ `root: bool = False` | **COMPLIANT** |

### Composite Agent Compliance (AGENT_LIFECYCLE §12.3/§12.4)
| Design | Implementation | Status |
|--------|----------------|--------|
| Two physical queues: inbox + gathering | ✅ `_queue` + `_gather_queue` | **COMPLIANT** |
| Partition by queue, never message type | ✅ `process_next` polls inbox then gathering | **COMPLIANT** |
| Fan-out: each branch gets loader-resolved `level_route`, `current_index=0`, `end_of_level_queue=gather`, `level+1`, fresh `branch_id` | ✅ `_fan_out` lines 183-241 | **COMPLIANT** |
| Re-key shared `input_as` under branch's first step's `input_as` | ✅ Lines 209-213 | **COMPLIANT** |
| Fan-in: `branch_id` names join slot | ✅ `_fan_in` lines 243-266 | **COMPLIANT** |
| Fan-in completion: group by `branch_id`, call `build_output_as` | ✅ Lines 268-291 | **COMPLIANT** |
| Resume: decrement level, advance `current_index` | ✅ `_resume_level` lines 310-324 | **COMPLIANT** |
| Gated collection: only poll gathering when pending | ✅ `_has_pending` gate in `process_next` | **COMPLIANT** |
| Anomaly on unexpected gathering message → visible error | ✅ Lines 154-158 (raises) | **COMPLIANT** |

### Tool Agent Compliance (AGENT_LIFECYCLE §4.10.4, §12.1)
| Design | Implementation | Status |
|--------|----------------|--------|
| Resolves `parameters` explicit `{input_as}.{key}` | ✅ `resolve_parameters` in tools.py | **COMPLIANT** |
| Loads tool from `available_tools` | ✅ `available_tools.load_tool` | **COMPLIANT** |
| Validates call against tool signature at execution | ✅ `validate_callable_signature` | **COMPLIANT** |
| Bounds output under agent's `output_as`/`output_schema` | ✅ `build_output_as` + `_verify_output_contract` | **COMPLIANT** |
| Failure → visible error result | ✅ Returns `{"error": error}` | **COMPLIANT** |

---

## 11. Summary of All Deviations

### Critical (Architecture Violations)
1. **Runtime doesn't implement gate exemption for error-result deposits** (AGENT_LIFECYCLE §12.5.5) — error deposits should bypass gate check.
2. **Legacy `legio.manager.submit`/`status` and `api.py` use `"local:"` task_ids and write directly to Manager's `tasks` scope** — known deviation, documented in LEG-085 open item 1.

### High (Rule Violations)
3. **Hardcoded `"local"` node_id default in Manager** (Rule 7: domain-free) — `src/legio/manager/__init__.py:47-48`
4. **Module globals `_db`, `_db_path`, `_node_id` in Manager** (Rule 2: semantic naming, anti-pattern) — `src/legio/manager/__init__.py:50-52`
5. **AgentBase `max_steps=100` hardcoded** (Rule 2, should be configurable) — `src/legio/agents/base.py:120`
6. **Manager `control_ttl=60.0` hardcoded** (should be configurable) — `src/legio/manager/__init__.py:135`

### Medium (Contract Gaps)
7. **`destroy_instance` terminal confirmation logic accepts `SUCCESS` incorrectly** — `src/legio/runtime/__init__.py:674-676`
8. **Runtime mints `task_id` without calling `validate_task_id`** — `src/legio/runtime/__init__.py:262`
9. **`_instance_tasks` map lost on restart** — `destroy_instance` raises `RecoverableError` (known open item, LEG-085 item 2)

### Low (Code Quality)
10. **Inconsistent citation style** — some files reference §5.8/§10.2, others ARCHITECTURE §7, etc. Mostly consistent now.
11. **Built-in constants duplicated** — `_BUILTIN_DRAIN_TIMEOUT`/`_BUILTIN_DRAIN_INTERVAL` in config.py (lines 122-123) vs used in LifecycleConfig.resolve.

### Test-Specific
12. **Tests use busy-wait `_expect_seed_success` with 200×0.01s** — acceptable for determinism but not production pattern.

---

## 12. Positive Findings (Compliant Areas)

✅ **LEG-083 Manager** — Fully compliant with contract  
✅ **LEG-084 Registry** — Fully compliant with contract  
✅ **LEG-086 Boot Catalog** — Fully compliant with contract  
✅ **LEG-085 Runtime** — Substantially compliant (minor gaps above)  
✅ **AGENT_LIFECYCLE §4.6/§5.8** — Drain/now destroy with bounded waits correctly implemented  
✅ **AGENT_LIFECYCLE §5.1-§5.9** — All lifecycle verb flows implemented per translate table  
✅ **AGENT_LIFECYCLE §12.3/§12.4** — Composite fan-out/fan-in correctly implemented  
✅ **Schema 2 FlowToken/Message** — All fields present and validated  
✅ **Rule 11 Logging** — Comprehensive structured logging at all observable points  
✅ **Rule 9 Errors** — Visible failures, no silent swallowing (except agent crash edge case)  
✅ **Rule 8 Polling** — Only documented exceptions (bounded clock waits) use sleep  
✅ **No layer writes another layer's scopes** — Verified by pinning tests  
✅ **Rule 3 Journal** — Committed every turn  

---

## 13. Recommendations

1. **Fix gate exemption for error-result deposits** — Add logic in composite/agent deposit paths to bypass gate check for error results (AGENT_LIFECYCLE §12.5.5).

2. **Remove legacy `"local:"` task_id path** — Unify `api.py` to use `Runtime.submit`/`status` (LEG-080/081 work).

3. **Make `max_steps` configurable** — Add to `LifecycleConfig` or agent config.

4. **Remove Manager module globals** — Use dependency injection throughout.

5. **Fix `destroy_instance` terminal predicate** — Should check for terminal state after cancel, not accept `SUCCESS` from bring-up.

6. **Validate Runtime-minted `task_id`** — Call `validate_task_id` after minting.

7. **Address `_instance_tasks` restart issue** — Either persist the map or handle gracefully (LEG-085 open item 2).

8. **Move built-in constants to class attributes** — `LifecycleParams.BUILTIN_DRAIN_TIMEOUT` etc.

---

*Audit performed against: ARCHITECTURE.md, AGENT_LIFECYCLE.md, PLAN.md, AGENTS.md, CONTRACTS/LEG-083/084/085/086.md, and the implementation in src/legio/runtime/, src/legio/manager/, src/legio/registry/, src/legio/config.py, and corresponding test files.*

---

## Tests run

- Audit is analysis only; no code changed, no tests run in this turn (the 291-test suite remained green from Session 80).

## Known issues / open points

- The 12 deviations listed above (2 critical, 4 high, 3 medium, 2 low, 1 test-specific) remain open. Work tree (code + contracts + docs from Session 80) remains uncommitted pending maintainer approval.
- Which deviations to fix in the next session is the maintainer's call; recommendations 1-8 are listed above.

## Next steps

1. Commit this journal (rule 3) — done.
2. On the maintainer's call: pick the deviations to fix (recommend starting with critical #1 gate exemption and #2 legacy "local:" path, then high items 3-6, then medium items 7-9). Re-run full suite + ruff + pyright after each fix.